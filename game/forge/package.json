{
  "name": "@gb/forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "preview": "node --experimental-strip-types tools/preview.ts",
    "plans": "node --experimental-strip-types tools/floorplan.ts",
    "measure": "node --experimental-strip-types tools/measure.ts",
    "reach": "node --experimental-strip-types tools/reach.ts",
    "playable": "node --experimental-strip-types tools/playable.ts",
    "generate": "node --experimental-strip-types tools/bundle-wording.ts && node --experimental-strip-types tools/emit-schema.ts"
  },
  "dependencies": {
    "@gb/kit": "workspace:*",
    "@gb/quest": "workspace:*",
    "@gb/world": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@gb/play": "workspace:*"
  }
}
