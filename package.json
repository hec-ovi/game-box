{
  "name": "game-box-workspace",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check:isolation": "node tools/check-isolation.mjs",
    "verify": "pnpm run generate && pnpm run typecheck && pnpm run check:isolation && pnpm run test",
    "generate": "pnpm -r --if-present run generate"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "@types/node": "^26.2.0"
  }
}
