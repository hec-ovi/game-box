/**
 * What kind of town a style word names. The reader lives in `@gb/world`,
 * because the generator reads the same word and the two must not disagree.
 */
export { flavourOf, type Flavour } from '@gb/world'
