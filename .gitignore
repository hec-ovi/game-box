# local-only raw idea and requirement docs (persistent, never published)
docs/IDEA.md
docs/REQUIREMENTS.md

# research store (may contain proprietary insights, default private)
.research/

node_modules/

# build output, anchored: an unanchored rule also swallows source folders that
# happen to be called build/ or dist/, and it does it silently
/target/
/assets/build/
/assets/dist/
/game/*/dist/
*.gguf
*.onnx
*.safetensors
.env
tsconfig.tsbuildinfo
assets/src/
