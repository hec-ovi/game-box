{
  "name": "@gb/forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "generate": "node --experimental-strip-types tools/emit-schema.ts",
    "skyline": "node --experimental-strip-types tools/skyline.ts",
    "measure": "node --experimental-strip-types tools/measure.ts"
  },
  "dependencies": {
    "@gb/kit": "workspace:*",
    "@gb/kitbash": "workspace:*",
    "@gb/quest": "workspace:*",
    "@gb/world": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@gb/play": "workspace:*"
  }
}
