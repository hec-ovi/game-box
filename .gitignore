# local-only raw idea and requirement docs (persistent, never published)
docs/IDEA.md
docs/REQUIREMENTS.md
docs/donotread.md

# reference images he drops in for a box to work from
docs/buildingstop/

# research store (may contain proprietary insights, default private)
.research/

node_modules/

# build output, anchored: an unanchored rule also swallows source folders that
# happen to be called build/ or dist/, and it does it silently
/target/
/assets/build/
/assets/dist/
/game/*/dist/
*.gguf
*.onnx
*.safetensors
.env
tsconfig.tsbuildinfo
assets/src/

# a pnpm store a container left behind when it installed over the mount
.pnpm-store/
