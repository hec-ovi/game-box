# Handovers between boxes

An agent finishes and hands work to a box it does not own. The coordinator has
to carry it. Two were dropped today and the owner noticed, because he had seen
the feature in a render and never in the game. This is the ledger that stops
that.

Checked against the repository, not against the reports. Delete a row when it
is done and the check confirms it.

## Ranked by what the player would notice

1. `forge` never places the wall-lean anchors `cast` built idles for
2. `app` never wired `dressing.room(interior)` and `room.decor`
3. Prop footprints disagree: forge plans a table 1.6x0.9, furnish draws 1.0x1.0, so chairs sit 0.4 m off the edge
4. `forge` serialises the narrator: peak concurrency 1, four of five llama slots idle, 85% of model wall clock
6. No way to give up a quest: three boxes did their half, the middle one was never asked
7. The journal cannot tell an unreached step from an open one
8. The boot panel clamps blocks to 24 where forge now accepts 77

## Outstanding

| # | Handover | From | To | What to do |
|---|---|---|---|---|
| ~~1~~ | done: 73 lean anchors, 29 propped people on a 10x10 town. Place `lean` anchors: 0.44 m out from the wall face, rot 180/0/270/90 per wall, 0.79 x 0.76 m clear, no `propId` | cast | forge | `lean` appears nowhere in `game/forge/src` |
| 2 | `dressing.room(interior)`, `built.root.add(room.decor)` | furnish | app | in flight |
| 3 | One shared prop spec (cells, contact, `onSurface`) with no renderer dependency | furnish, forge | world + forge + furnish | forge cannot import furnish without putting `three` in the headless generator |
| 4 | `Promise.all` over `namePlace` and `describeNpc`/`describeItem`, reassembled by index | scribe | forge | `forge.ts:161/225/250` await one at a time |
| 6 | `HudIntent` needs `{ kind: 'abandon'; questId }`, and a "Give up" beside Follow | app, quest | hud | `Game.abandon` is wired and casts for an intent that does not exist |
| 7 | `QuestStep` carries one boolean where the engine has three states | app | hud | |
| 8 | `BLOCKS.max` is 24; forge's derived bound is 77 | forge | app | |
| 9 | Anchor the bartender against `staffContact` (1.00), not `contact` (1.10) | furnish | forge | `staffContact` is published and unreferenced |
| 13 | Interior surfaces should carry metre UVs like ground meshes do | furnish | scene | would remove furnish's shader projection entirely |
| 18 | The world contract's outputs table omits half of what it publishes | nav | world | |
| 19 | `surroundings.md` has no line for `work-bench` | world | talk | |
| 20 | `nav/tests/contract.test.ts:125` assumes a bar exists | forge | nav | |
| 21 | `nav` prices a mountain cell impassable; `land` calls it walkable verge | forge | nav + land | |
| 22 | `sources.json` claims KayKit was retargeted (30-bone rig, nothing ships) | cast | assets | |
| 23 | `downloaded.json` still records two removed packs | furnish | assets | |
| 24 | No `CHANGELOG.md` | world | repo | project rule asks for one |
| 25 | The design a plot got is not in the world file, so growing the catalogue re-skins shared cities | prefab | world + forge + bundle | the first thing to revisit when those boxes open |
| 26 | Tall towers need forge-marked landmark sites with a raised `maxStoreys` | prefab | forge | the catalogue is 1-4 storeys because that is what forge cuts |
| 27 | Bounce light: walls stay near-black because nothing bounces | prefab | app | the biggest remaining gap against the references |
| 28 | An interior probe so a reflective floor reflects something | furnish | furnish | named as a next task |
| 29 | Overhead wires derived from lamp positions | assessment | kitbash | cheap "inhabited" trick, never done |
| 30 | `KitDressing.building()`'s `door` empty has no consumer | scene | kitbash | dead surface |
| 31 | `SceneCast` recycles bodies by kind, so street variety is capped by the pool | cast | crowd + app | 360 male and 180 female looks exist, far fewer show |
| 32 | Stale lines in `PENDING.md` and `PLAN.md`, including `viewOf` citations for code now deleted | audit | docs | all landed or never true |
| 33 | `talk/src/credit.ts:26` emits `talked` with no topic, so a topic'd step still cannot complete through a real conversation | scene-batch agent | talk | the objective now names the topic; talk must forward it |
| 34 | `game/app/src/spawn.ts` is now redundant: `city.spawn` does the same thing and lands square on the cell centre | scene | app | delete it and use `city.spawn` |
| 35 | A car is placed on a sidewalk cell on the current forge layout | scene-batch agent | traffic | its own test catches it |
| 36 | Root `package.json` carries `pnpm.onlyBuiltDependencies`, which pnpm no longer reads and warns about on every run | scene-batch agent | repo | |
| 37 | Never hand a userland `undici` dispatcher to the built-in fetch: Node 24.19 bundles undici 7.29, a userland 7 Agent works, an 8 Agent is rejected outright | sidecar | repo | pinning `^7` works today and breaks on the Node that bundles 8 |

| 38 | Cast's rotation table gives `rot 270` for the west wall under "90 faces -x"; forge's `dirOf` has 90 facing **+x**. The physical facing is right because forge used its own `inward(side)`, but the two write-ups are mirrored on the east-west axis and one is wrong | forge | cast + forge | an east-west mirror already cost this project real time once; settle which convention is the true one and correct the other document |

## From the browser playthrough

A quest was completed end to end twice, once with the model and once offline.
These are what it found on the way. Numbered by what a player hits first.

Rows 39, 40, 41, 42, 45 and 46 landed in `app` and were checked in a browser:
five walkers out of fifteen with somebody left in every shop, a `collect` step
pinned and routed from indoors and out, and `?sidecar=` surviving a refresh.

| # | Defect | Box | Detail |
|---|---|---|---|
| 43 | A timed quest is an invisible real-time stopwatch | forge + play + hud | a timer is ~1.5 to 2 real minutes, one model reply is 8 to 19 s of it, and nothing on screen says a quest is timed or how long is left. One expired mid-playthrough and the first sign was an empty journal |
| 44 | Every carried thing is the same beige cube | scene + furnish | box, envelope, stained glass, ledger and cash all render identically |
| 47 | "It belongs to somebody, and they are standing right there" is shown as the hint while the player is across town | forge | `src/quests/recipes/hot-parcel.ts`; the hint reads as if the player were in the room |
| 48 | A conversation opens with a blank panel and the NPC says nothing until the player speaks first | app + talk | decide whether a greeting is worth a model call; `@gb/talk` would own the line |
| 49 | `TalkPatch.acted` cannot be cleared | hud | `acted` appends within a panel and only a **new** `speaker` resets it, so a per-turn line is impossible from outside. `app` announces what the speaker did instead, which leaves `acted` with no consumer: either take `acted: null` to clear it, or drop the field |
| 50 | "Nothing yet. Find someone to talk to." reads as if the player had never had a job | hud | `src/surfaces/objectives.ts`; true after finishing one and being offered two more |
| 51 | **A room can be cut in half by its own furniture, and a chair can sit on the doorstep** | forge | measured over 121 interiors in 15 generated cities: 5 put a prop on the spot 1.2 m inside the street door, and 8 of 365 stationed people plus 7 of 242 items end up in a pocket of floor the player cannot walk to. `g/Golden Wheel Coffee` seats tables at x=1.60 and x=4.00 with chairs at 0.45, 2.85 and 5.15, leaving aisles of 0.40 to 0.50 m against a 0.70 m body, so a row of tables seals the room. Identical with `@gb/scene`'s `Greybox` and with `@gb/furnish`, so it is the layout and not the drawing: the plan has to reserve an aisle a body fits down and keep the doorstep clear |

## From the wider roads

| # | Defect | Found by | Box | Detail |
|---|---|---|---|---|
| 52 | One flat car cap for a whole city | scene+traffic+crowd agent | app | `game/app/src/street.ts:118` passes `maxCars: 12` for the entire town. A four-lane avenue holds twice the cars a street does, so the number that made a street look right leaves an avenue empty. It wants to scale with the lane network, not with the city |
| 53 | `@gb/hud`'s `QuestStep` widening breaks `game/app/tests/contract.test.ts` under `tsc -p game/app` | scene agent, then confirmed live | app | `done` becomes optional, and the app fixture types it as required. This is the migration the hud agent owes; it lands with hud's report, not separately |

## From the panel

Rows 49 and 50 landed in `@gb/hud`, along with the journal's third step state
and the abandon control. What they leave for their callers:

| # | Work | Box | Detail |
|---|---|---|---|
| 54 | Push `acted` per turn | app | `acted` is now the line for the turn in front of the player: sending it replaces, `acted: null` clears, omitting keeps. Push it when `@gb/talk` emits `did`. The assertion at `game/app/tests/contract.test.ts:1132` that app never sends `acted` goes with it |
| 55 | Send `state` on each `QuestStep` | app | `'upcoming' \| 'open' \| 'done'`. `done` still reads, so nothing breaks until app moves |
| 56 | Bind `Game.abandon` to `HudIntent.abandon` | app | The hud asks twice on its own; do not add a second confirm. After `QuestLog.abandon`, push `quests` without that quest, the hud drops nothing itself |
| 57 | Publish a per-quest step list carrying each step's state | quest | The three states are derivable today only by walking `QuestLog.toJSON()`'s `open[]` and `done[]` against the quest doc's step order. Publishing it saves every caller that walk and stops them disagreeing |

## From the things you carry

Row 44's furnish half landed: all 25 archetypes are built shapes, three casts
each, one shared material, no draw added.

| # | Defect | Found by | Box | Detail |
|---|---|---|---|---|
Row 58 is closed. Scene did not read furnish's published `contact(prop)` and
gave a measured reason: scene cannot import furnish (furnish depends on scene),
and one number per prop kind fixes 20 of 42 cases and cannot fix the rest, since
it cannot say which of a bar counter's two tops a thing is over. A ray at the
point the thing is going gets 42 of 42 with nothing published at all. Over 88
placements in 8 towns: resting on nothing 87 to **0**, not even over its own
piece 60 to **0**.

## From the journal and the greeting

`@gb/quest` publishes `QuestLog.journal()`, and `@gb/talk` speaks first. Row 57
is closed. What they leave:

| # | Work | Box | Detail |
|---|---|---|---|
| 59 | There is a fourth step state, `dropped` | hud | A step on a branch nobody took: the far side of a `choice`, and the rivals an `any-of` beat. `QuestStepState` has three. Either render it or declare hud never receives one; until then app throws those steps away and a quest that split reads as if the untaken side never existed |
| 60 | `QuestEntry.title` against `Objective.questTitle` and `JournalEntry.questTitle` | hud | Two names for one string across one boundary, renamed by hand at every caller |
| 61 | Push `opening.line` and `opening.moves` when a conversation opens | app | `game/app/src/talking.ts`, around the `Conversation.open` call at line 70. Talk's half of row 48 is done and costs 0.007 ms; until app pushes it the panel is still blank |
| 62 | Read `log.journal()` instead of walking `toJSON()`'s `open[]` and `done[]` | app | `game/app/src/reporting.ts`. hud's field is `title`, quest publishes `questTitle`, so the mapper renames until row 60 lands. Drop `dropped` steps until row 59 lands. `game/bundle` keeps using `toJSON()` for the save file, which is what it is for |

## From the narrator fan-out

Rows 51 and 47 are closed in forge; rows 59 and 60 are closed in hud.

| # | Work | Box | Detail |
|---|---|---|---|
| 63 | `Scribe.namePlaces` has no caller | scribe | A facade no longer costs a model call, so the plural naming method is dead. Also `InstanceRequest`, `InstancePost` and `InstanceStock` now carry an `index` (the caller's own numbering, so an offline narrator can reproduce its draws); scribe still compiles without it |
| 64 | Forge plans a 1.6x0.9 table, furnish draws 1.0x1.0 | forge + furnish | Still open, deliberately. Forge over-reserves, which is the safe direction. It needs the two boxes to agree one number, not a change in either alone |

| # | Work | Box | Detail |
|---|---|---|---|
| 65 | `game/app/tests/contract.test.ts:634` asserts the glass at `x = 10.1`; it is now **9.2** | app | "Beside them" follows the anchor's own facing (45 cm to their right) instead of world +x. Same 0.45 m, one constant. Caused by scene's fix and the only break outside its folder |

## From the choice pass

| # | Work | Box | Detail |
|---|---|---|---|
| 66 | Never announce or render a `step-opened` change whose `hidden` is true | app | `objectives()` and `journal()` filter hidden steps; the change stream cannot, because the app has to know the step is on the board. The flag is there so the caller stays quiet. Switching to `journal()` alone does not close the spoiler if app announces changes |
| 67 | `drive.ts` emits `chose` from the quest document rather than from a published objective | forge | The objective now publishes the option keys, so the harness can answer with the same list a player is shown |

| 68 | Route the panel's `decide` intent into the engine's `chose` event | app | `{ kind: 'decide', questId, stepId, optionId }` maps field for field onto `log.handle({ kind: 'chose', ... })`. Then push the new `journal()` and `objectives()`; the hud marks nothing itself. The last leg of the choice blocker |

## From the topic pass

Row 33 is closed, and so is the free-text decider. `@gb/talk` credits a topic
only when the NPC is put to that subject through the menu, never by a
conversation that wanders onto it. The one-line fix the audit imagined (forward
the first open topic on every `talked` event) fails both of its tests.

| # | Work | Box | Detail |
|---|---|---|---|
| 69 | `Member.gesture()` has no caller, so NPCs talk with their arms still | app | Not talk's to make: talk holds no body, has no three.js and no `@gb/cast` dependency, and pulling cast in would put a renderer inside a headless box. App owns both ends already. `member.gesture(...)` on a `said` piece for `conversation.npcId`, `stopGesture()` on `over`. Two lines, nothing new needed from either contract |

| 70 | A thing left somewhere is not drawn there | scene | The put-down credits and the item leaves the player's hands, and nothing appears on the surface. A `Pickup` handle's batch matrix is baked at `put()` time, and `PropSurface`/`Pickups` are not exported, so there is no published way to place an item at an anchor after the build |
| 71 | `JournalEntry` has no `kind`, so nothing marks the main line | quest, then hud | Forge generates it correctly. App cannot add it without reintroducing the hand mapper it just deleted |
| 72 | A `complete` step is a journal line reading like a task | quest or forge | `journal()` lists every step in document order. Either the journal filters flow-control steps as a rule, or forge's recipes are writing task-sounding text into a `complete` objective |

| 73 | An openable door looks the same as a door that never opens | world + forge, then prefab | Prefab can carry a second door layer (0.7 MB) and pick between them inside `orient`, which already rewrites the geometry per plot, so the choice costs nothing beyond the layer. It needs one flag: `plot.opens` on `@gb/world`'s `Plot`, set in the same forge pass that decides which plots get interiors. With most buildings shut, a door you can actually use should read as one from the pavement |

## From the pavement

`@gb/crowd` published `SceneCast.members()`, the same shape and key as
`CastDressing.members()`, so one lookup reaches a person at a post indoors and a
passer-by on the street. It also fixed two live bugs found on the way: a body
parked mid-gesture came back still talking with its hands and wore that down the
street on the next walker, and somebody who joined the player got two bodies
because `follow` spawned a companion without retiring the street walker.

| # | Work | Box | Detail |
|---|---|---|---|
| 74 | Gesture calls for a person stopped in the street | app | `bodies.members().get(npcId)?.gesture(...)` on the turn, `stopGesture()` when it ends. **Look it up every frame; never cache the `CastMember`** — bodies are recycled, and a cached member is a stranger's arms. `Street.populate` types its parameter as the port, which has no `members()`, so keep the concrete `SceneCast` on the field you call from |
| 75 | Pass `at: { x, z }` from `walkers()` when a passer-by becomes a companion | app | Otherwise they teleport to the player instead of stepping off the pavement where they stood |
| 76 | Treat `held === false` and `members().get(id) === undefined` as the conversation ending | app | A held walker is still retired past `retireRadius`, deliberately: an app-leaked hold would otherwise pin a walker forever. Walking 70 m away with a panel open ends the person, not just the hold |

## From the save

`@gb/play` now carries `where`, `tracked` and `moved`. A thing put down survives
a reload, and cannot be in a hand and on a shelf at once: `take(itemId)` forgets
the spot, so the app cannot fail to hold the invariant up.

| # | Work | Box | Detail |
|---|---|---|---|
| 79 | Call `player.place(itemId, { interiorId, anchorId })` on the put-down | app | The same two ids the `stashed` event already carries. It replaces the `drop` call rather than joining it: it takes the thing out of the inventory and clears its stolen mark itself |
| 80 | Dress a room from `player.placed()` on resume, and skip the world file's own placement for any item id it names | app | `room.leave(itemId, anchorId)` is already published by `@gb/scene`, so this is app-side only. An old save returns `[]` and the world file is the fallback as before. A save naming a room this city has not got loads; recover it with `player.place(itemId, null)`, which puts the thing back where the city file had it |
| 81 | Place everyone in `player.companions()` beside `player.where` on resume | app | The fact of companionship is saved; only the metres were missing, and those are now derivable from the player's own place. Without it a companion resumes at their post across town and either walks the city to catch up or snaps to the player on the first frame |
| 82 | A save written while the clock is paused opens frozen forever | app | `P` pauses by setting the rate to 0, and the rate it was running at lives in app memory rather than in the save. Reload and time never moves, the sun never sets, nothing on screen says why |

### Decided, so nobody reopens it

**An opening line may reach a street NPC's back, and that is accepted.** The
kerb rule means up to a couple of seconds between the hold and the person
turning to face you. `@gb/crowd` deliberately publishes no "they have turned"
signal and `@gb/app` deliberately does not wait for one: the defect being fixed
was a blank panel, and a two second wait to avoid a cosmetic oddity trades a
real fault for a smaller one. Somebody answering while they finish crossing
reads as calling back over their shoulder. Companions are exempt from the rule
entirely, so this only ever applies to a stranger on the pavement.

## From the road

Row 78 is closed. The braking rule was already in `@gb/traffic` and already fed
by the app; two ways of running somebody over had survived it. A car now stops
at anybody it could not brake for (0.5 m short, not zero, because zero would let
the walking side's push-out shove a body down the street), and stays out of a
junction somebody is standing in the middle of, which used to let one car hold
every arm for 24 seconds. Cost on the case that is actually the game, people on
pavements: 2 to 3.5x cheaper than before, and now scaling with the crowd rather
than crowd times roads.

| # | Work | Box | Detail |
|---|---|---|---|
| 83 | `street.ts` passes `PERSON_CLEAR` (0.34 m) as a person's radius to traffic | app | That is the body-collision capsule, not a person's width in the road. It makes the hazard band 1.24 m either side of a lane centre, so a car passes 0.56 m from somebody's middle without slowing: it clips a shoulder. Leave `radius` out, the port defaults to 0.5, or pass 0.5. One word, and it is the difference between clearing somebody and shaving them |
| 84 | `near()` allocates an array plus one object per person per frame | app | Traffic's contract now permits reusing one array and mutating its entries. Small, and it is the app's frame |

## From the clips

Row 77 is closed. 16 clips became 28, every wrong stance is fixed, and
`GESTURES` went from two talking loops to four (a nod and a head shake), so a
conversation can answer as well as talk. Pack 0.62 MB to 0.99 MB.

The lasting part is a second build-time maker: `tools/anims/blend.mjs` lays one
clip's movement over another's stance, the same sum the runtime gesture layer
does. Measured from the movement's own first frame it adds only the movement, so
a seated body drinks. That is where `sit-drink`, `sleep` and `work-desk` came
from, with no purchase. What it cannot do is invent a limb the source never
moves, or reach a *place*: anything that must reach is a measured pose.

| # | Work | Box | Detail |
|---|---|---|---|
| 85 | A sleep anchor is placed for a body sitting up | forge | `stance.ts` puts it 0.43 m toward the headboard, the seat rule. A lying body is centred on its root, so the anchor belongs at the middle of the mattress; today a sleeper lies at the right height with their head past the headboard. Place against: centred, 0.96 m either side, height carried by the clip |
| 86 | The bed is 6 cm too short | forge + furnish | Pad 1.84 m, a body with boots is 1.90 m end to end |
| 87 | `work-desk` has no reach band | forge | Staff at a counter have one (0.15 m); a seated desk worker has none. The wrists sit at 0.78 m, 0.20 to 0.24 m in front of the root, so the desk edge belongs under them |
| 88 | `METRICS.worktopHeight` is 0.9 and every standing clip on this rig reaches 1.02 | world | A cook's hands ride 12 cm over the hob. Either the worktop rises to counter height or that stance needs a clip nobody sells. Same family as the bar-counter fault |

### Decided: the fabric tile goes on roughness, not colour

`@gb/cast` measured it properly and found only the third objection real. Download
is not the problem: multiplied into a repainted sheet and re-encoded, a sheet
goes 23 KB to 33 KB, about **+0.24 MB across the whole 13 MB character pack**.
The UV objection is answered: the garments are properly unwrapped, 2,365 distinct
UV cells over 3,006 body vertices, so a tile reads as a pattern rather than one
stretched swatch.

The real objection is the map. Multiplied into base colour it is a *printed*
quilt that does not respond to light, which is the cloth the owner said he does
not want. His words are "a sheen that moves, not cloth", and a sheen is a
specular property. So: **roughness (or normal), not base colour**, masked per
fabric region so the face and hands on the shared sheet are not quilted. The
sheets already exist in the same UV layout and `tools/wardrobe/fabrics.mjs`
already names each region, so it is the same one-step multiply.

## From the answer

Row 89 is closed at the talk end. `@gb/talk` publishes
`{ kind: 'answered', answer: 'yes' | 'no' }`, only on turns that have an answer,
so "neither" is the absence of the event and nothing defaults to agreement. The
rule that keeps the three tracks from drifting: **carrying a move out is a yes**,
decided in the one place the model track, the offline reader and a clicked move
all converge. A reported refusal that still picks a move counts as a yes,
because what happened is the ground truth.

| # | Work | Box | Detail |
|---|---|---|---|
| 90 | Play the nod and the shake on `answered` | app | One case in the `TalkEvent` loop. `@gb/cast` needs nothing |
| 91 | The nod may land visibly late with the model up | talk | `answered` arrives at turn settle, because the voice call and the action call run in sequence and one call decides both. Overlapping them is a restructure of the two-track flow inside talk. Only worth doing if it reads late on screen; app must not compensate for it |
| 92 | No clip sits a body on a raised seat | cast | Every `sit-drink` anchor on a bar stool is 33 cm out, four of them in a nine-plot city. The only seated clip has soles on the floor, body underside 0.423, against a 0.75 pad. Lowering the stool is worse (a 0.42 m seat at a 1.1 m bar). `@gb/forge` anchors `sit-drink` on chairs meanwhile |
| 93 | Worktops, hobs, sinks and the lowest shelf ledge rise 10 cm | furnish | `METRICS.worktopHeight` went 0.9 to 1.0, measured: standing palms land at 0.972. Furnish reads `METRICS` directly and its tests measure drawn triangles against it, so they follow; what needs a look is anything drawn **under** a worktop (a cabinet run) and whether a shelf's ledges still fit |
| 94 | Seed the pause from `player.clock.paused` on resume, and call `clock.resume()` when `P` unpauses | app | Closes row 82. The save now carries `rate` (the running rate, always above 0) and `paused` separately; `setRate(0)` still pauses and `setRate(n)` still runs, so the toggle keeps working, but the rate it resumes at has to come from the clock and not from app memory, or a paused save opens frozen with the app believing it is running. An old save with `rate: 0` opens paused at `DEFAULT_RATE` |
| 95 | Pay with `player.pay(amount)` for a `pay` effect | quest | `spend` is the same call and goes once quest has moved. `pay` refuses a price that is not a whole number zero or more (`invalid-amount`) as well as one not held; both deduct nothing |
| 96 | Give each background fact an id | forge | `player.unlock(npcId, factId)` and `unlocked(npcId)` carry fact ids as strings and the codex on screen looks them up in the world. A staged background is a list of `{ id, text }` per person, with the id stable across regeneration of the same seed |
| 97 | Sell through `player.buy(itemId, price)` | app | One call: it pays and takes, and refuses (`already-carried`, `invalid-amount`, `not-enough-money`) with nothing moved. The price is the world's or the counter's to state; play holds no prices |
| 98 | Record what a conversation changes in the person | talk | `player.remember(npcId, fact, 'told' \| 'seen')` for a sentence the person now holds (12 per person, oldest dropped, 200 characters each), `player.warm(npcId)` / `cool(npcId)` for how the turn went, `player.unlock(npcId, factId)` when a background fact is earned, and `player.discover({ npc })` when a conversation opens. `player.memories(npcId)` and `disposition(npcId)` are what to put in front of the model for that person; nothing spreads to anyone else |
| 99 | Note places entered with `player.discover({ place: interiorId })` | app | On walking through a door. `discovered()` then lists places and people in the order first found, for the codex |
| 100 | A codex window from `player.discovered()` | hud | `{ places, people: [{ npcId, unlocked }] }`: the ids are the world's, so names come from the world file and a person's fact texts from their staged background (row 96); `player.disposition(npcId)` gives the standing to show per person |

## Checked and closed

About fifty handovers landed and were verified in the code rather than taken on
trust. Nine more were **wrong**, and those are the interesting ones:

- `sidecar`'s timeouts were reported unwired. They are wired, proved against a
  real server with eleven mutation checks. The report was stale when filed.
- "Crowds can no longer cross a street" was inferred from a constant and
  disproved by measuring the running city: 47 of 50 walkers stood on a roadway
  cell. The real defect was *where* they crossed.
- `kitbash` was asked for a merged shadow stand-in worth 3.3 ms. `scene`'s
  batching made it unnecessary; adding it would duplicate geometry for nothing.

The lesson in all three: a claim about another box is a hypothesis until
somebody measures it.
| 94 | `mountain` is not in the world contract's cell vocabulary | world | nav | `nav` prices `mountain` impassable (`WALK_COST` infinite) and its contract says so; `game/world/CONTRACT.md` names `BUILDING_KINDS` and `ROAD_KINDS` but no `CellKind` list, so nothing outside nav can read that a mountain is impassable. Publish the cell kinds with a one-word meaning each |
| 95 | Emit `companion-arrived { npcId, place }` when a companion's body enters a plot or interior | app | An escort is credited by that event alone; `arrived` with the companion flag set moves nothing. Hypothesis, unmeasured: app sends only `arrived` today, so every escort stalls until this lands |
| 96 | Drive an escort in the harness with `companion-arrived` for the step's `npcId` at its `place` | forge | `companion-join` followed by the player's `arrived` leaves the step open now, by design: the table under "What credits a step" in quest's contract names the one event per kind |
| 97 | Re-pick every `time-limit` against game seconds at 24x | forge | Quest's contract, "Timers": 24 game seconds per real second, a day is one real hour. Budget 600 per conversation and 3000 per walk, nothing under 3600. A value picked for the 240x clock is now ten times shorter in real time |
| 98 | Push `journal()` on every `clock` event, and after `quest-failed` | app | `timer.remaining` on a timed page moves only when the page is re-read; the failed page stays in the journal with `status: 'failed'` and `failReason`, so push the list with it in rather than without it |
| 99 | Draw the countdown and the failure reason on a journal page | hud | `timer: { remaining, total }` in game seconds (divide by 24 for real seconds) on a timed page while it runs; `failReason` (`fail-step`, `time-limit`, `npc-lost`, `item-lost`) in plain words on a failed one, which today reads as an absence |
| 100 | Send `talked` with `topic` when a conversation covered a talk step's published `topic` | app | The objective carries `topic`; a topic step is credited by the same one and nothing else. Until app sends it, generators keep emitting no topics |
| 101 | Push the conversation as turns, with `does` apart from `says` | app, talk | hud | `TalkPatch` takes `turns: [{ who: 'you' \| 'them', says, does? }]` to replace the transcript whole, and `reply`, `replyChunk`, `does` (string, or `null` to clear) edit the speaker's current turn. `acted` reads as `does` meanwhile (`game/app/src/talking.ts:128,141` and `game/app/tests/contract.test.ts:1530` send it). When `@gb/talk`'s tool carries `does` and `says`, hand `does` to `hud.show({ talk: { does } })` before the reply streams and the panel draws it as stage direction |
| 102 | Route the settings intents and the way out | app | hud | `HudIntent` has `lock-time { locked }`, `skip-time`, `weather { weather }` and `exit`. Map them onto the same calls `P`, `T`, `K` and `N` make today. `N` is the hud's key now (`HUD_KEYS.leave`): it reports `exit` and stops at the capture listener, so the app's own `N` binding never fires; drop it and act on the intent. Push `settings: { hour, minute, locked, weather, weathers }` on every clock tick and weather change so the tab reads right |
| 103 | `window: 'items'` is `window: 'inventory'`; the purse corner is gone | app | hud | `HudWindowName` is `quests \| map \| inventory \| codex \| settings \| controls`. Money and what is carried are read in the inventory only, so keep pushing `money` and `carrying`. The codex (row 100) takes `codex: { entries: [{ id, kind: 'place' \| 'person' \| 'history', title, text }] }`: app turns `player.discovered()` ids into names and a line each, the hud draws words only |
| 104 | Drive the loader from scribe's progress port | app | hud | `hud.show({ loading: { title, stages: [{ id, label, state: 'waiting' \| 'running' \| 'done', done?, total? }] } })` on every progress event, `loading: null` when the city is ready. Scribe publishes `stage` (`city`, `instances`, `quests`) with `done` and `total`; the history is the premise call before them. Name the stages in the player's words: writing the history, laying out the city, writing the places, writing the quests |
| 105 | Announce a busy model apart from a failure | app, sidecar | hud | `hud.announce({ kind: 'model-busy', retryIn: seconds })` on a 429 or a backoff, drawn as a wait with the seconds counting down and kept up for the whole wait; `hud.announce({ kind: 'error', text })` on a real failure, drawn warned. Never the same notice for both |
| 106 | The controls tab lists what the game pushes: put G, T, K and P on it | app | hud | The hud lists its own keys (J, M, I, X, O, ?, N, Esc, Enter, Tab). `patch.controls` must carry the game's: G (the way to the tracked quest), T (turn the time of day), K (change the weather), P (hold the clock), and the rest of `game/app/src/controls.ts`. Do not list N or I there: the interface owns them |
| 107 | Call `land.setTime` every frame with the fractional hour, and carry the environment off `land.light` between prefilters | app | `land.setTime(clock.secondsOfDay / 3600)` in the frame loop, not on the hour turn: every value the land writes is now a smooth function of the real hour (measured under a hundredth of a light unit a frame at 1/900 h). Between prefilters set `environmentRotation.y = land.light.sunYaw - yawAtFilter` and `environmentIntensity = base * land.light.skyBrightness / brightnessAtFilter`; refilter on the hour and on a weather change. `skyBrightness` is the dome's mean radiance in its own units, 1.59 at the temperate noon, 0.019 at midnight |
| 108 | Give the day its own grade and exposure | app | The dome at noon is not flat: zenith `(0.10, 0.33, 0.95)`, horizon `(1.30, 1.79, 1.94)`, a five to one gradient, and the sun casts at 3.1 with the shadow map at full strength from 08:00 to 16:00 (temperate). What `daylight_cartoonish.png` shows on top of that is exposure and grade, which is a hypothesis this box cannot measure. The day is now short and low (07:25 to 16:35, noon 24 degrees, warm morning, cold afternoon) so the grade has nine hours to cover, not twelve |
| 109 | `DAY_PHASES` and the `T` key's four stops sit off the sun | play, app | Sunrise is 07:25 and sunset 16:35 on the temperate theme (06:32 to 17:28 arid, 07:44 to 16:16 maritime), so `dawn` at 05:00 to 06:59 is full night and a jump to `dusk` at 17:00 lands after the sun is down. `land.light.sunrise` and `land.light.sunset` publish the hours if the phases or the stops want to follow the sky |
| 110 | The verge now meets the pavement at 0.15 m, the road at zero, and rises away from both | scene, nav | A kerb scene draws against a `mountain` cell down to zero is buried inside the verge and can go; the `mountains` stand-in ring is unaffected. The verge is a bank of slope under 0.1, walkable, which is row 21's disagreement with nav restated with numbers |
| 125 | Light a downward-facing surface: a ceiling fill | scene, app | Measured off furnish's probe: it lays 0.079 (corpo) and 0.045 (home) on an upward face and 0.0084 and 0.0056 on a downward one, so a matte ceiling lit by it alone reads 0.0006 and 0.0015 linear, black, and the probe cannot give more without becoming a light (its picture averages under a tenth by contract). Floors and walls on screen are lit by the sun, the sky and the moon ambient at ten to twenty times that, and none of it reaches a downward normal: the prefiltered sky's lower half is black by design (land contract line 88) and `Land.skyLight` is taken down (app contract line 84). A fill on downward normals at about a third of the ambient (a hemisphere ground term, or the environment's lower half painted with the ground's bounce) is what makes a ceiling read; furnish lifted the corpo lid to 0x4a4d52 (0.076 linear, from 0.020) so a fill can show it. Hypothesis, unmeasured in the game frame: the same fill lights soffits and the undersides of balconies outdoors |
| 126 | Publish the interior's `finish` so furnish stops reading the building kind | world | Furnish dresses a room in `finishOf(interior.kind)`: `home` for apartment, house and hotel, `corpo` for the rest (`game/furnish/src/style/finish.ts`, exhaustive over `BUILDING_KINDS`). PLACES section 2's `finish` (`domestic`, `civic`, `industrial`, `corporate`, `worn`) replaces that table the day the interior carries it |
| 127 | The mattress is 1.970 m of level pad in corpo and 1.934 m in home, measured off the drawn triangles | forge | Forge's contract line 62 says 1.837 m and a 4.2 cm overhang at each end; a 1.92 m body now lies inside the pad in both languages, so that text is stale. Row 86 is closed on furnish's side and its test holds the pad at 1.92 m or more |
| 128 | `docs/PIPELINE.md` line 203 says interior props are still the Fantasy MegaKit and the Dungeon Pack | docs | Measured by grep over `game/furnish`: every one of the 24 props, 25 carried things and 7 shelf things is drawn through `Solid.block`; the one model file the box reads (`interior-kit.glb`) carries four tiling images and no mesh. The row is stale and the feature in PENDING ("Theme-appropriate props indoors") is done |
| 129 | Give each car a `footprint` in the crowd's `Hazards` feed | app | `@gb/crowd` 0.8.0 keeps walkers out of cars: `{ x, z, vx, vz, radius: 2.25, footprint: { length: METRICS.vehicle.carLength, width: METRICS.vehicle.carWidth, heading: car.heading } }` per `@gb/traffic` car; the heading is the car's own `rotation.y`, either nose convention. Without the footprint a car is a 2.25 m circle, which still keeps people out of it but pushes them off the kerb lane's side. Include the player's parked car (`@gb/drive`) at speed zero so nobody walks through it and no companion is placed in it. `near()` is now asked once a frame at about 80 m plus the kerb looks, so one reused array is fine |
| 130 | Somebody indoors who agrees to come along: `crowd.follow({ npc, door: plotId })` | app | `plotId` is `world.interior(npc.station.interiorId).plotId`. The body appears on that plot's doorstep (`entrance.cell`) and walks over to the player. No `at` needed; `at` still wins when given. A plot with no doorstep on the pavement sets off from the player |
| 131 | Where a walker is heading, for the conversation | app, talk | `crowd.destination(npcId)` is `{ plotId, arrived }` or undefined (nowhere in particular, or a companion). `world.plot(plotId).name` is the place; `arrived` means they are standing at its door. Ask it when the conversation opens. The errand text is the generator's (`life.errand` on the `Npc`, forge); the crowd publishes only the fact |
| 132 | `held === false` is the one end signal, and it now fires when the player walks off | app | Once the player has been within `talkRadius` (5 m, `crowd.options.talkRadius`) of a held walker, going further than that ends the hold the same frame, and the walker goes on their way. If the app talks to people from further off than 5 m, pass a larger `talkRadius` or the hold ends at once. Indoors, the crowd has no body: `new Leash(crowd.options.talkRadius)`, `reset()` on open, `gone(player.x - post.x, player.z - post.z)` each frame, `talking.end()` when it is true |
| 125 | Read `turn` instead of `said`, and draw `does` apart from `says` | hud, app | `@gb/talk` publishes `{ kind: 'turn', does?, says }` for every spoken turn, `does` being a few words of stage direction ("wipes the counter") and `says` the speech. `said` still carries the same words for the move across and goes once nothing reads it. `TalkPatch` needs a place for `does` that does not read as dialogue |
| 126 | One `Sessions` per playthrough, `does` to the gesture layer, `learned` to the codex | app | `new Sessions()` from `@gb/talk` once per playthrough and pass it as `sessions` on every `Conversation.open`, so walking back up to somebody carries on their own transcript and nobody hears another's. On a `turn` event hand `does` to the gesture layer (prose, may map to nothing). `open` now returns `learned` (fact ids earned by meeting) and turns publish `learned { npcId, factId }`: refresh the codex on both |
| 127 | A background fact's id is its index in `npc.background`, as a string | hud | `'0'`, `'1'`, ... counted from 0, which is what `player.unlocked(npcId)` and `learned.factId` carry. The codex draws `npc.background[Number(id)].fact` |
| 128 | Measure whether one `Conversation` or one npc id is held across openings | app | Reproduced inside talk with the sidecar up on 2026-08-25: two `open` calls in two buildings sent two briefs naming the right person, room and spot each time, and talk keeps nothing across speakers. So the stale-context report ("answers as if in the previous building") is not talk's lifetime. Hypothesis, unmeasured: the caller reuses a `Conversation` or the npc id it opened last. Print `conversation.npcId` on every E press against the person in reach |
| 129 | Write `life.reason` and `life.errand` as one sentence the person could say out loud, first person | forge, scribe | Talk says `reason` as the greeting's middle beat exactly as written (first letter capitalised, full stop added), so "I'm covering the day shift while Rook is away" works and "she covers the day shift" does not. The city built for talk's live measurement (seed `talk-live`, 14 people, 2026-08-25) carried 0 `life` and 0 `background`: row 113 is still open, and until it lands every person answers off `personality` and `knowledge` alone |
| 130 | `Anchor.doing`, the free phrase from PLACES.md stage 1 | world | Talk fills "what you are doing" from the anchor kind today and prefers `life.reason` for the greeting; once the schema carries `Anchor.doing` talk reads it for the stance line, for the speaker and for everybody else in the room |
| 133 | Push `compass: { facing, goal }` from the body's yaw and the guide | app | hud draws a strip along the top: `facing` in radians clockwise from north, `goal: { label, bearing, distance, line }` for the tracked quest with `bearing` in the same unit (the compass point of the route's first stretch, as `G` answers it), `distance` in metres along the walk, `line` the quest's `kind`. Push it on every turn of the player and every re-resolve of the guide; `compass: null` with no city and indoors, where the route is measured from the door |
| 134 | Mark the plan's goals with `line`, pin every live quest, and name the plots that earn it | app | `MapMark` on a goal takes `line: 'main' \| 'side'` (left out reads as side) and the hud draws the two apart, so pin the open steps of every quest with work, not only the tracked one. `MapPlot` takes `label` (read on hover) plus `named: true` for the ones written on the plan: places in `player.discovered().places`, quest targets, and landmarks; and `prominence: 'background' \| 'notable' \| 'landmark'` from the charter once `@gb/world` publishes it (PLACES.md stage 1), left out reads as background |
| 135 | The codex is pushed as places, people and history, never as `entries` | app | `codex: { places: [{ id, name, text }], people: [{ id, name, role, disposition, facts }], history?: [{ id, title, text }] }` from `player.discovered()`: `disposition` is `player.disposition(npcId)`, `facts` is every staged fact of the person (row 96) as `{ id, text }` for the ids in `unlocked` and `{ id }` for the rest, so the hud can draw the locked ones as locked. Replaces the `entries` shape in rows 100 and 103 |
| 136 | Take `lights?(plot, size): LightEmitter[]` on the `Dressing` seam and light walls from it | scene | `@gb/prefab` publishes `PrefabDressing.lights(plot, size)`, asked after `building` for that plot: `kind` (`entrance`, `screen`, and `@gb/kitbash`'s `sign`, `strip`, `doorlamp` for the signs it hung), `position` metres in the building's own frame 20 cm off the lit face, `colour` packed `0xRRGGBB`, `intensity` candela at full dark, `radius` metres (0.1 lux, at most 16). Scale by `city.night`; the pack has no point lights of its own and the emissive pass carries the look alone today (PENDING 447-477, 205-229). `@gb/kitbash`'s `lightsFor` is the same shape, so one type serves both dressings |
| 137 | The door columns were the pack's own tubes, not the kit's; closed | kitbash | Measured on pack 1.5.0: `corpo-a-6x10x2` carried a cyan tube at x 0 from 0 to 4 m through the middle of its door, `shop-b-8x12x2` magenta tubes at x plus or minus 1.0 on a door 2.2 m wide. Pack 1.6.0 has no tube below the parapet on any of 512 models. Kitbash's door lamps are the only lit thing beside a prefab door now; nothing to do there |
| 138 | `sit-drink` now sits at `stoolHeight`: put its anchors back on bar stools | forge | `Sitting_Stool_Loop` and the three clips posed from it put the hips on a 0.75 pad with the chair clip's 2.7 cm of give and the chair clip's root-to-hips offset (0.33 m behind the root), so a stool anchor is placed exactly like a chair anchor 0.30 m up. A `sit-drink` anchor left on a chair floats its body 0.30 m; a bar's `sit` anchors stay chairs. Cast contract, "Sitting on a stool", measured on all twelve dressed characters |
| 139 | The stool's rail: soles rest 0.37 m under the pad, 0.38 m off the floor | furnish | The seated feet tuck back under the seat (ankles 0.18 m ahead of the hips, knees at 56 degrees) and want a rail at 0.38 m to stand on; a bar stool drawn with its rail there reads as sat on. Cast contract, "Sitting on a stool" |
| 140 | Where the new browse and work-bench clips reach | forge | `Crouch_Idle_Loop` (on `browse` and `lean`) puts the head 0.21 m ahead of the root at 0.84 m and the hips 0.28 m behind it; `Farm_Harvest` (on `browse`) dips a hand to 0.28 m at 0.31 m ahead of the root; `Kneel_Fix_Loop` (on `work-bench`) works both hands at the root at knee height and kneels from 0.5 to 1.0 m behind it. A browse anchor closer than 0.25 m to its shelf face, or a bench with less than 1.0 m of floor behind its anchor, puts a body through furniture. Cast contract, "Reaching a work surface" |
| 141 | Draw walks from `walkFor(npcId)` and pace them with `member.pace(metresPerSecond)` | crowd | `WALKS` is `Walk_Loop` and `Walk_Formal_Loop`, drawn per id so a street is not in step; `GAITS` carries every moving clip's authored ground speed (walks 0.98, carry 0.65, push 0.30, jog 5.9, sprint 8.9 m/s) and `pace` scales the clip toward the body's real speed, held between 0.7 and 1.4. `Push_Loop` comes with a trolley in front (the handle at 1.17 m, the cage 0.58 to 1.28 m ahead of the root): give it to the odd courier at 0.3 m/s. Cast contract, "Moving along" and "Things in hands" |
| 142 | Call `member.attend(point)` when a conversation opens on a stationed person and `member.resume()` when it closes | app | A bent-over or leaning body comes up to the relaxed idle and turns to the point inside its object (the object's position and heading are never touched); a seated body stays seated at its own height and turns its head and chest, or stands up through `Sitting_Exit` when the point is behind it; a sleeper stays lying. `lookAt` alone leaves a desk worker bent over the desk with the face turned. Cast contract, "Being spoken to" |
| 143 | `stoolHeight` now carries a stance | world | Contract line "One height carries no stance at all": `Sitting_Stool_Loop` sits the hips on a 0.75 pad (underside 0.723, the same give as the chair) with the soles 0.37 m under it. Add the row to the reach table: `stoolHeight`, sat on a stool with the feet on a rail, hips on the pad, soles at 0.38 |
| 144 | Hero bodies wear the plain bodies' clothes until outfits are cut for them | cast | Pass 2. `chooseCharacter` hands `hero-male` a `male` outfit and `hero-female` a `female` one (`tests/wardrobe.test.ts`); both hero files in `wardrobe.json` are the Superhero bodies the plain kinds already use, so a hero body needs its own body file and outfits before it looks like anything else |
| 145 | `BODY_KINDS` names four bodies; the shipped pack has two | world | Measured on `Universal Base Characters[Standard].zip`: `Superhero_Male_FullBody` and `Superhero_Female_FullBody` are the only bodies in it (glTF for Godot-UE and FBX for Unity, the same two), and the "four" are those two meshes with a Light and a Dark skin sheet each under `Textures/`. `game/cast/wardrobe.json` already builds `male` and `female` from those two files, so `hero-male` and `hero-female` are the `male` and `female` mesh under another name: cast dresses them off the plain rails and they look the same. Contract line 196 ("the four bodies of the shipped pack") is stale; by its own rule (every value maps to something the game can render) the two hero kinds either stay as names for the plain mesh or come out of the closed list. Cast compiles either way: `RAIL` in `game/cast/src/wardrobe.ts` is exhaustive over `BODY_KINDS` |
| 146 | Writing `hero-male` or `hero-female` in `populate` spawns, dresses and animates, and changes nothing on screen | forge | Cast contract, "The bodies": a hero base dresses from the plain rail of the same sex (guard, works, courier and the other nine outfits), same mesh, same heights. Write them for the heavy roles only if world keeps the kinds (row 145); a bouncer looks heavier the day a pack with a second build is on disk, and cast then cuts a rail for it |
| 147 | `tests/support/stub-cast.ts` fails `pnpm run typecheck` against `CastMember` 0.8 | crowd | `StubMember` lacks `holding`, `pace`, `attend`, `resume`, `attending` (TS2420 at line 19, TS2739 at line 65). The full shape is the contract's Outputs row for `spawn`; `pace` and `attend` are rows 141 and 142 |
| 148 | Keep everybody in `questTargets(log.objectives())` at their post when sending a third of the town out | app | `@gb/forge` publishes `questTargets(objectives)`: every `npcId` on an open line of the board. Measured on the 8x8 harness town with the harness's copy of the app's rule (roster order, a third out, never the last one out of a room): 43 of 43 quests completable at their posts, 43 of 43 with those people kept in, 23 of 43 with nobody kept and 21 jobs sent to an empty room. Re-read the set whenever the board changes, which is when a step opens |
| 149 | Send a `clock` reading before a quest is taken, and on every tick after | app | The quest log counts a timer from the last `clock` it heard. Measured in the harness: a log that heard no clock before `start` failed a 15,300 s escort on its first tick (the reading jumped from nothing to 29,400); one told the clock first finished it. The harness sends `clock { seconds: state.clock.totalSeconds }` right after `QuestLog.create` and after every verb |
| 150 | A counter sells what it owns: build the buy from `item.ownerNpcId` and `item.value` | app, hud | Every item lying in a place with a `serve` anchor belongs to the person on it (`ownerNpcId`) and carries `value`, whole credits, the price that counter sells it for (`src/prices.ts`: a cup 3, a crate 40, a gem 300, moved up to 30% by the seed). An item with no owner is not for sale. Taking an owned item without paying is the theft the quests already use. Measured on a 5x5 neon town: 34 items, 34 priced, 19 sellers |
| 151 | Fill `life` and `background` on the people `describeNpc` and `writeInstances` write, and keep the singular path's names unique | scribe | `NpcProfile.life` is `@gb/world`'s `Life` (history, interests, manner, cares, avoids, reason, errand; `reason` and `errand` first person, one sentence talk says out loud) and `NpcProfile.background` its staged facts (`met`, `talked`, `quest`, `told`). `namePlace` and `describeNpc` are now handed `premise` (the story as `premiseLines` renders it). Measured on 2026-08-25 through the sidecar, a `Scribe` asked only its single-place questions on a 2x2 town: 16 people, 0 with a life, and two named "Kaelen" (the singular `describeNpc` has no roster; the plural path's uniqueness does not reach it). The offline narrator writes both fields for everybody; `src/narrator/lives.ts` and `background.ts` are the reference shape |
| 152 | Hand `brief` and `asks` to the narrator | forge | `writePremise({ theme, seed, brief?, asks? })`: scribe puts the owner's `brief` in the history prompt verbatim and `asks.tone`, `asks.mainQuest` and `asks.style` beside it; and `WorldSummary.asks?` (`summarise` reads it off `world.asks()`), which the quest writer reads for `mainQuest` (the main line's call), `sideQuests` (each side errand's call) and `tone` (every call). Both are optional on scribe's side already; a summary without `asks` asks nothing of the model |
| 153 | Name the shut buildings through `namePlaces` when the narrator has it | forge | `Scribe.namePlaces(requests)` takes `{ kind, theme, index, street?, premise? }` per plot (`index` is the plot's own number, `street` the road its door is on when the plan knows it, `premise` as `premiseLines` renders it) and hands back one sign per request in request order, twenty to a model call, with the history in front of the model and no head word twice across the city (the model's own signs and the ones the offline composer mends alike). Measured on one live 1x1 town: 9 signs, 9 distinct heads, five shapes (The X Y 3, possessive 3, family firm 1, number 1, plain trade 1), 7.8 s for a batch of 5. The offline composer stays the fallback for a sign the model repeats or a batch it will not write |
| 154 | The loader's stage ids are now `history`, `city`, `places`, `quests` | app, hud | `ScribeStage` renamed: the history is its own stage (1 answer), `city` is the name and then every sign batch (one bar that grows: 0/1, 1/1, then 1/41 ... 41/41), `places` the instances, `quests` the quests. Every stage ends with `done === total` and each event carries `what` |
| 155 | A forced `tool_choice` is not enforced on the local path | host | Measured on 2026-08-25 against llama-server through the sidecar with the quest tool (both the 10,779-character compacted schema and the 37,854-character expanded one) and with a 400-character flat schema: of 13 calls, 6 came back as `finish_reason: stop` with a ```json block in `content` and no `tool_calls`, and the ones that did call the tool wrote `next` as a string against a schema that says array. Which way a call goes is fixed by the seed (the same seed always gives the same shape), so it is a sampling draw and not the schema. Hypothesis: the engine is not handed a grammar for the forced tool. Scribe now retries prose on the next attempt's seed; a grammar on the host would make every attempt count |
| 156 | Build the mix, the storeys, the rooms and the stock off `world.charters()`, and declare the charters before the first plot | forge | PLACES stage 2. `World.found({ charters })` or `world.recordCharters(list)` before `addPlot`: a plot whose word no charter declares is refused (`unknown-reference`), and a file that carries `charters` is read against those alone, so the presets a town uses go in the list beside the invented ones (`SHIPPED_CHARTERS` is the fourteen, resolved). Three things the charter shape does not carry, measured while transcribing: hall sizing (`foyer` for restaurant and office, `lobby` for clinic and hotel) is not a field, so key it on the hall's `use` or the storeys; `STOCK`'s pool order is not reproducible through `HOLDING_ARCHETYPES` (bar's `bottle, glass, ledger, cash` becomes drink plus papers plus valuables, eight archetypes), so the golden hash of invariant 2 moves on items unless the preset's pool is drawn another way; a bar's `Cellar` and a shop's `Back room` keep their labels through the charter room's `kind` (`roomKindOf(spec)`), the rest cut to `ROOM_USE_KIND[use]`. Every preset is `access: open`, which is every door today |
| 157 | Read `built`, `signage` and `blade` off `world.charter(plot.kind)` | kitbash | `RECIPES`, `SIGNAGE` and `TRADE_WORD` are typed on `BuildingKind` and `plot.kind` is now a word: `src/compose/plan.ts` and `src/sign/plan.ts` fail `pnpm run typecheck` (TS7053, TS2345). `ResolvedCharter.built` is the `Recipe` shape exactly (`street`, `flank`, `upper` as `{ plain, window, rhythm }`, `crown?`, `fascia`, `door`, every piece in `KIT_PIECES`), `signage` is the `Signage` shape, `blade` the word. The fourteen presets carry today's rows verbatim |
| 158 | Read `tint` off the charter and pass `built`, `signage`, `blade` and `tint` through the `Dressing.building` seam | scene | `src/dressing.ts` `BUILDING_TINT` indexes `Partial<Record<BuildingKind, number>>` by a word and fails typecheck (TS7053). `world.charter(plot.kind).tint` is the same packed colour, presets carry today's fourteen |
| 159 | Match looks on `charter.suits`, not on a kind enum | prefab | `src/catalogue.ts` fails typecheck (TS2345: a word is not a `BuildingKind`). `suits` is sorted lowercase tags; every preset's list holds its own word (`bar`, `house`) plus its frontage, material, sprawl and prominence, so a look tagged with the fourteen words keeps matching |
| 160 | Type `PlaceRequest.kind` and the premise prompt on the word, and ask the model for charters against `CharterSchema` | scribe | `tools/measure.ts` fails typecheck (TS2322: `kind: string` into `PlaceRequest`); `src/premise.ts` joins `BUILDING_KINDS`, which is now the preset word list only. `charterContract.jsonSchema()` is the tool schema for a charter: twelve enums, two clamped numbers, bounded text |
| 161 | Dress rooms from `finish` and `room.use`, and keep today's picture while doing it | furnish | `src/dressing.ts` and `tests/contract.test.ts` fail typecheck (TS2345: `interior.kind` is a word). `world.charter(interior.kind).finish` is `domestic` for house, apartment and hotel and `corporate`, `civic` or `industrial` for the other eleven, so `home` for `domestic` and `corpo` for the rest is today's table; `roomUseOf(room, charter)` names the routine for a file that left `Room.use` out. Closes row 126 |
| 162 | Read `anchor.doing` for the stance line when it is there | talk | `Anchor.doing` is optional free text, 300 characters, on every anchor in the file; the forge writes it when a charter's room wants a phrase. Closes row 130 |
| 163 | `MapPlot.prominence` comes from `world.charter(plot.kind).prominence` | app, hud | `background`, `notable` or `landmark` (`PROMINENCES`); the presets mark station and chapel `landmark`, bar, clinic, hotel and market `notable`. Closes the world half of row 134 |
| 164 | Read `interior.finish` for the language a room is dressed in | furnish | `@gb/world` 0.11.0: `Interior.finish` (`FINISHES`: `domestic`, `civic`, `industrial`, `corporate`, `worn`) is on every interior a `World` hands out (`interior(id)`, `interiors()`), written from the charter by `addInterior` for a new city and read off the charter for a file that left it out; the schema keeps it optional because a file from before may lack it. Today's table is `domestic` for house, apartment and hotel and the rest `corporate`, `civic` or `industrial`, so `home` for `domestic` and `corpo` for the other four keeps the picture. Closes row 126 at the world end; row 161 stands for the dressing |
| 165 | Cite the cell vocabulary instead of holding one | nav, land, scene | `@gb/world` 0.11.0 publishes `CELL_KINDS` (seven kinds) and CONTRACT.md "The cells a city is laid in": per kind, what walks it, what drives it, what it is drawn as, with the mountain rule under it. Closes row 94. Hypothesis, not measured: `game/scene/CONTRACT.md` line 63 puts a `mountain` cell at zero and kerbs the pavement against it, which the published rule (rise from the pavement top, no kerb) contradicts, as row 110 already says |
| 166 | `BODY_KINDS` is `male` and `female`; drop the hero rails | cast | Measured after the change: `pnpm run typecheck` fails at `src/wardrobe.ts:62` and `tests/pack.ts:25` (TS2353, `'hero-male'` is not a key of `Record<BodyKind, BodyKind>`). The contract's body table (lines 77 to 80) lists `hero-male` and `hero-female` against the same two files. Closes rows 144 and 145 |
| 167 | `bodyFor` writes a body kind the world refuses | forge | Measured after the change: `src/populate.ts:52` fails typecheck (TS2322, `'hero-female' | 'hero-male'` is not `BodyKind`), and the schema now refuses `appearance.base` outside `male` and `female` at `load`, so a city written with a hero body does not open. Contract line 72 ("a quarter of the guards, workers and mechanics get the hero build") describes what the pack cannot draw. Closes row 146 |
| 168 | `schema/bundle.json` moved with the world schema | bundle | `pnpm run generate` from the root rewrote it (two bodies, optional `Interior.finish`); `pnpm -C game/bundle test` is 15 of 15 green against it. Nothing to change, it only has to land in the same commit as `game/world` |
| 169 | Drop `spend` from PlayerState | play | quest 0.8.1 pays a `charge` effect with `pay(amount)` and calls `spend` nowhere (grep of game/quest/src: 0 hits); row 95 closed |
| 170 | Move the `T` key's four stops onto the sun | app | `@gb/play` phases now sit on `@gb/land`'s temperate sun: `dawn` 06:00-07:59, `morning` 08:00, `dusk` 16:00-17:59, `night` 21:00-03:59, and `isDark` runs 18:00 to 05:59. It exports `SUNRISE_HOUR` (7) and `SUNSET_HOUR` (16); make the stops `SUNRISE_HOUR - 1`, 12, `SUNSET_HOUR` and 21 (or read them off `DAY_PHASES`' first hours) so a jump to dusk lands with the sun still up |
| 171 | Send `does` on `TalkPatch` for what the speaker does; `acted` goes once nothing sends it | app | `hud.show({ talk: { does } })` before the reply streams, `does: null` to take it off the turn; the hud draws it as stage direction on the speaker's current turn (hud 0.10.1, tested). `acted` is read the same way meanwhile. Hypothesis, unmeasured from hud: row 101 named `game/app/src/talking.ts:128,141` as the senders of `acted` |
| 171 | Walkers ask `pace` for 1.19 to 1.61 m/s over walks authored at 0.98 | cast | Measured on the fixture town, 32 walkers, one minute of frames, `walkFor` and `pace` wired: 103,552 pace calls, median 1.33 m/s, mean 1.28, max 1.58; 46% of them above 1.37 m/s, which is the 1.4 cap on a 0.98 walk, so the brisker half of the street skates by up to 0.21 m/s. The speeds come from `METRICS.player.walkSpeed` (1.4) and `speedSpread` 0.15. A walk authored nearer 1.4 m/s, or a cap that reaches 1.65 on the walks, would end the skating; the crowd hands over the true speed either way |
| 173 | Hand `room()` the interior's charter | app | `@gb/furnish` 0.11.0: `room(interior, charter?)` takes `world.charter(interior.kind)` beside the interior and reads a room's use off it (`roomUseOf`) when the file left `Room.use` out, which is every room a generated town carries today (measured on the furnish test town: 5 interiors, 5 with `finish`, 0 with any `room.use`). Without it a room draws its wall bays from the finish's row alone and its use tilts nothing. Hypothesis, not measured: the app's interior build calls `dressing.room(interior)` with one argument |
| 174 | Write `Room.use` on every room a city is built with | forge | Measured on the furnish test town (`Forge` + `OfflineNarrator`, 3x3 blocks): 5 interiors, 0 rooms carrying `use`. `@gb/furnish` reads `room.use` first and `roomUseOf(room, charter)` second, so today every room goes through the charter; a room stamped at build reads the same either way (a furnish test holds the two byte for byte) |
| 175 | Hand `building` and `lights` the plot's charter as a third argument | scene | `@gb/kitbash` 0.9.0 closes row 157: `KitDressing.building(plot, size, charter)` and `lights(plot, size, charter)` take a `PlotCharter` (`Pick<ResolvedCharter, 'built' \| 'signage' \| 'blade'>`, so `world.charter(plot.kind)` whole fits). Measured after the change: `src/dressing.ts:28` fails typecheck (TS2416, `Dressing.building` provides two arguments and kitbash wants three) until the seam carries the charter; every other file under `game/kitbash` is clean and 55 of 55 tests pass. A plot handed over without its charter is not a wall with holes any more: a piece the library lacks is refused (`kit-unknown-piece`) and a signage row outside its bounds too (`signage-out-of-range`) |
| 176 | Pass the charter into `signsFor` and `lightsFor` | prefab | `@gb/kitbash` 0.9.0: `signsFor(plot, size, charter, cellSize?)` and `lightsFor(plot, size, charter, cellSize?)` read `signage` and `blade` off the charter; `SIGNAGE` and `TRADE_WORD` are gone with `BuildingKind`. Hypothesis, not measured: `PrefabDressing.building(plot, size)` and `lights(plot, size)` (prefab contract lines 14 and 15) call both with two arguments and fail typecheck; the charter arrives through the same seam row 175 asks scene for |
| 177 | Write `built` for a generated charter from `RECIPES[frontage][openness]` | forge | `@gb/kitbash` 0.9.0: `RECIPES` is `Record<Frontage, Record<Openness, Recipe>>`, eighteen rows, `Recipe` being the `built` shape `ResolvedCharterSchema` carries; measured against `SHIPPED_CHARTERS`: thirteen presets are their row verbatim, the chapel keeps `Brick_Window_Trim_Single` on the masonry row, and every `blank` row glazes nothing (the window slot holds the wall). Hypothesis, not measured: the resolved-field writer (PLACES section 6, forge stage 2) has no other source for `built`, and `game/forge` declares no dependency on `@gb/kitbash` today (INDEX edges), so consuming it adds `game/kitbash <- game/forge` |
| 178 | Carry the plot's `ResolvedCharter` through `Dressing.building(plot, size, charter)` and `lights(plot, size, charter)` | scene | `charter` is `world.charter(plot.kind)`. Prefab 0.7.0 reads `charter.suits` for the pick and hands the charter on whole to the dressing behind, which reads `built`, `signage`, `blade` and `tint` (row 158). Measured with the seam at two parameters: root `tsc` lists 6 errors under game/prefab (TS2416 `src/dressing.ts:56`, TS2554 at `:59` and `:72`, TS2345 `tests/dressing.test.ts:91,99,135`), `KitDressing` is not assignable to `Dressing` either (the `:99` one), and `pnpm -C game/prefab test` fails 1 of 46 because `buildCity` reaches `PrefabDressing.building` with no charter. All of it clears on the three-argument seam |
| 179 | Pin with `catalogue.design(plot, size, world.charter(plot.kind)!.suits)` | cli | Prefab 0.7.0: the pick keeps the models whose `tags` share a word with the charter's `suits`, so `design` and `designFor` take them as a third argument. The pack is 1.7.0 (models carry `tags`, no binary changed), so the `catalogue.identity` that `build` writes as `requires` has a new manifest hash |
| 171 | Take `charter` on every place request, and `charters` back off `writePremise` | scribe | Forge 0.22.0: `PlaceRequest`, `InstanceRequest`, `namePlace` and `describeNpc` carry `kind: Word` (any charter word, `jail` included) and `charter: Charter` (`@gb/world`'s shape: label, names, rumours, service, work, holding, rooms), so a prompt can say what a jail is here. Measured: `pnpm run typecheck` fails in scribe on `namePlaces` because scribe's `PlaceRequest` types `kind` as `BuildingKind`. `writePremise({ theme, seed, brief?, asks? })` may answer a `History` (`Premise & { charters?: Charter[] }`, `historyContract` exported from forge): a charter dictionary tool whose parameters are `CharterSchema`, and forge drops any charter that fails with its reason on `ForgeResult.dropped`. `WorldSummary.asks` is on the summary `writeQuests` gets |
| 172 | Print `dropped` from `Forge.build` in the build report | cli, app | `ForgeResult.dropped: { word, reason }[]` is every kind of place the history declared that the city would not take (a bad axis, rooms nobody can stand in, past the twenty-fourth). PLACES section 4: dropping without reporting is the failure the gate exists to remove, so `gb check` and the loader should show it |
| 173 | A founded world's `toJSON` leaves `finish` off interiors that `load` then writes back | world | Measured on forge 0.22.0 before it wrote `finish` itself: `World.found` then `addInterior` without `finish`, `toJSON`, `load`, `toJSON` again: the second document carries `"finish":"civic"` on every interior and the first does not, 253,876 bytes both, differing at the first interior. Contract line 135 says the file is left as written. Forge writes `finish: charter.finish` on every interior now, so nothing of forge's depends on it; a file from an older generator still round-trips two ways |
| 180 | Every forced call on the local path comes back as the call; measure how often the prose retry fires now | scribe | host 0.5.0 (closes row 155). Measured on llama-server b10603: a named `tool_choice` is read as `auto` (19 of 20 prose with a "write it as JSON" line, 5 of 5 "Hello." on a hello prompt) and `required` cannot end a reply the model resists (3 of 5 past 100 s, 4,531 tokens in 90 s on the hello prompt). So a server of your own is sent the tool's parameters as `response_format: json_schema` with `tool_choice: none`, and the JSON its grammar writes is read back as the call: 20 of 20 non-streamed and 5 of 5 streamed forced calls came back as the call on the prompt that gave 19 of 20 prose, `next` an array in every one, 11 to 17 s each. `required` with one tool is forced the same way (5 of 5); `required` over several tools and `auto` go out unchanged. A reply an engine still writes as prose carrying a JSON block that fits the parameters comes back as the call with `salvaged` on the reply. Hypothesis, not measured: the seed-per-attempt retry on prose never fires on the local path now, and the "next as a string" arguments are gone with the grammar |
| 181 | The reply may carry `salvaged`; row 106's shape stands | sidecar | host 0.5.0: `chat-response.json` and `chat-stream-event.json` gain optional `salvaged` (integer, at least 1), on the response, and on the chunk carrying the call, when the call was rebuilt from prose the engine wrote instead of calling; a reader that refuses unknown top-level fields must allow it. Confirmed against the contract and `schema/api/error.json`: HTTP 429, `type: rate_limit_error`, `code: model-busy`, `Retry-After` in whole seconds, exactly as the sidecar reads them; nothing changed there |
| 182 | Call `city.lights.follow(camera.position.x, camera.position.z)` every frame | app | `@gb/scene` 0.12.0: `CityBuild.lights` is a `CityLights`, `LIVE_LIGHTS` (16) `THREE.PointLight`s under `city.root` handed to the emitters nearest that point (decay 2, distance the emitter's radius, intensity its candela times `city.night`, so noon is off). Built, they stand round the spawn, so the first frame is lit before the call. Measured on the Radeon 8060S at 1920 by 937 on the two block default city, greyboxed, render pass on the GPU: 16 live lights 2.20 ms against 0.90 with none on WebGPU, 2.33 against 1.06 on WebGL2; `CityOptions.lights` moves the budget. The ceiling fill (row 125) is a `DirectionalLight` named `fill` inside `buildInterior().root`, so a room brings it: nothing to add. Hypothesis, not measured in the game frame: the same fill on the city root would light soffits and the undersides of balconies outdoors |
| 183 | Hand `building` the charter and `surface` the size | cast | Measured after `@gb/scene` 0.12.0: `game/cast/src/dressing.ts(34,23)` TS2554 (`building(plot, size, charter)` wants the plot's `ResolvedCharter`, `world.charter(plot.kind)`) and `(50,23)` TS2554 (`surface(part, size)`, `size` a `SurfaceSize { u, v }` in metres) |
| 184 | Pass `size` through `surface(part, size)` to `rest` | kitbash | Measured after `@gb/scene` 0.12.0: `game/kitbash/src/dressing.ts(88,23)` TS2554, one argument where the seam carries two. `lights(plot, size, charter)` returning `readonly LightEmitter[]` with `position` `[x, y, z]` is the shape the seam takes: no change there |
| 185 | Pass `size` through `surface(part, size)` to `rest` | prefab | Measured after `@gb/scene` 0.12.0: `game/prefab/src/dressing.ts(109,23)` TS2554. `lights` is already the seam's shape |
| 186 | Take `surface(part, size)`, and read the tiling off the uv: interior surfaces carry metre UVs | furnish | Measured after `@gb/scene` 0.12.0: `game/furnish/src/dressing.ts(95,94)`, `(99,23)`, `tests/contract.test.ts(37,18)`, `tests/walls.test.ts(213,89)`, `(225,93)` TS2554 (`surface` takes `size: SurfaceSize { u, v }` metres; `room()` builds through `building(plot, size, charter)`). Every floor, ceiling and wall `buildInterior` makes now carries uv in metres, the ground's rule: a floor's `u, v` are `x, z` on the ground, a wall's `u` runs along it and `v` climbs, continuous from one wall into the next, so `repeat = 1 / tile` tiles at real size off the uv alone. Hypothesis, not measured: the world-position context in `src/surfaces/tiling.ts` can go (PENDING "Interior surfaces should carry metre UVs"). Row 125 closed on scene's side: a `DirectionalLight` `fill` at 1.6 shining up lights only what looks down; a lid painted `0x4a4d52` reads 0.043 to 0.051 linear (sRGB 0.23 to 0.25) under it alone, measured on the game's renderer, so the probe stays a reflection |
| 187 | Say when a city was written before charters, and refuse `unknown-kind` by its words | app, cli | `@gb/bundle` 0.4.0: `Bundle.open` returns `upgraded: true` for a `schemaVersion` 1 file: it was read against the fourteen presets it was drawn with, which are now in `world.charters()` with a `use` on every room, and its `contentHash` is still the file's, so its saves resume whole. Exporting that city again writes a `schemaVersion` 2 file carrying the charters (`pack` always writes 2; `BundleDoc.schemaVersion` is `1 \| 2`), with a new hash. The new error `unknown-kind { words }` is a file whose plots use a kind of place it does not declare: nothing loads, and the words are the message ("this file's places are of a kind it does not describe: jail") |
| 188 | A forced call through the service runs past 300 s on the local path; straight at the engine the same request ends in 14 s | host | Measured on 2026-08-25 with scribe's charter tool (`@gb/world`'s `charterContract` with `word` pinned, 3.8 KB of schema, a 2,800-token prompt): through the service, 9 of 16 charter attempts across two live 3x3 builds hit scribe's 300 s clock, and the engine log shows those slots decoding without end (`n_gen = 7616` at 24.8 t/s when the task was cancelled at 300 s), while the same seed, temperature and messages sent straight to llama-server as `response_format: json_schema` finish at 457 to 525 tokens in 11 to 14 s, 7 of 7, alone or five at once. Reproduces alone through the service too: `word: customs`, seed 3988743720, temperature 0.9, the sidecar's request shape (`tools` plus a named `tool_choice`), ran to undici's 300 s headers timeout while `jail` beside it answered in 42 s. Hypothesis, unmeasured from here: what the service adds to the engine request (the tools rendered into the gemma4 template beside the `response_format` grammar) lets the model carry on after the object closes. Scribe retries a `timeout` and a `broken` on the next seed, so a runaway costs a build 300 s per attempt and the charter stage was two thirds of a 954 s build |
| 189 | Quest calls still come back as prose on the local path | host | Measured on the same two builds: `write_quest` 16 calls, 8 came back with no `tool_calls` and the JSON in `content` (`{ "id": "quest_0001", "kind": "main", "title": "The Manifest Key", ...`), no `salvaged`, so the salvage in row 180 did not read them; scribe retried on the next seed and every quest was written. Charter, sign, place and city-name calls came back as the call every time |
| 190 | Hand `street` on `PlaceRequest` | forge | Scribe's `namePlaces` prompt shows each shut building as `a bar on Kettle Row` when the request carries `street`, and `a bar` when it does not; forge 0.22.0's `PlaceRequest` has no `street`, so the live signs are named without one. The plan knows which band a door is on; a street name per band (`Kettle Row`) is what the sign writer wants |
| 191 | Hand the charter on through `CastDressing.building(plot, size, charter)` | cast | Measured on 2026-08-25 after app moved to the three-argument seam: `game/cast/src/dressing.ts:34` calls the dressing behind with two arguments, so `charter` is `undefined` at `@gb/scene`'s `Greybox.building` (`reading 'tint'`) and 3 of app's `tests/starting.test.ts` cases built against `CastDressing` fail on it; in the browser every building of the live city falls back to blocks (`the art pack could not build buildings (reading 'suits')`, once per plot). Row 183 stands; app's own dressing wrapper (`guarded.ts`) carries the charter and the surface size |
| 192 | Carry `does` on `Turn`, so a transcript reopened shows what they did as well as what they said | talk | `history()` measured on 2026-08-25: `[{ role: 'assistant', content }, { role: 'user', content }]`, words only. App pushes the whole transcript as `turns` on every opening (hud's `TalkTurn` takes `does?`), so the stage direction of a past turn is lost between two openings of the same person |
| 193 | Record what the player was told of the city, for the codex's History heading | play | `discovered()` is `{ places, people }`; hud's `CodexView.history` (`[{ id, title, text }]`) has nothing to be pushed from, so app leaves it out and the heading never draws. A `discover({ history })` naming a premise `common` fact, or a `told` fact from talk, is the shape the tab already reads |
| 194 | Publish the speed on `Driving.car`, or a velocity beside it | drive | App feeds the player's car to `@gb/crowd` as a `Hazard` with its footprint so nobody walks through it, but `Driving.car` is `{ x, z, heading }` with no speed, so the feed says it is standing still whether parked or driving. Hypothesis, unmeasured: a walker at a kerb reads a car the player is driving at them as stopped (under the crowd's `hazardSpeed`) and steps out in front of it. One number on `Rolling` and the app passes it straight through |
| 195 | `CastDressing.building` and `.lights` call the dressing behind with one argument short | cast | Measured 2026-08-25 in the working tree: `pnpm run typecheck` reports `game/cast/src/dressing.ts(34,23)` and `(50,23)` against `@gb/scene`'s current `Dressing`, and every app test that builds a `Game` with `CastDressing` dies in `Greybox.building` on `charter.tint` of `undefined` (4 of 183 in `game/app`); the browser falls back to blocks with "the art pack could not build buildings (reading 'suits')". Row 191 names the same seam; this is what it costs the game today |
| 196 | Push `does` off `history()` when reopening a transcript | app | `Turn` is `{ role, content, does? }` since talk 0.10.0; hud's `TalkTurn.does?` takes it straight. Closes row 192. The `said` event is gone from `TalkEvent`: `turn.says` carries the words (grep of game/app and game/hud on 2026-08-25 found no reader of `said`) |
| 197 | Feed the hazard speed from `Driving.car.speed` | app | `Driving.car` is a `Moving`: `x`, `z`, `heading` and `speed` in metres per second along the heading, negative in reverse, 0 parked. Pass it straight through to the crowd hazard instead of 0. Row 194 |
| 197 | Push `history()` into `CodexView.history` and feed `told(text)` | app | play 0.6.0 keeps what the player was told: `player.told(text)` for each premise line and announcement shown, `player.history()` reads them back oldest first (strings, capped at `HISTORY_CAP` 60). App maps each line to `{ id: String(index), title, text }` for hud. Closes row 193 |
| 197 | A charter whose `frontage` and `material` are both `masonry` fails the whole build | forge | Measured 2026-08-25 through `gb build --history` with a hand-written `customs` charter (`street.frontage: masonry`, `material: masonry`, every other field valid): `Forge.build` answers `invalid-brief` with `charters.5.suits: no tag twice` and nothing is built. The resolved `suits` carries the word, frontage, material, sprawl and prominence, so the shared word lands twice and `@gb/world` refuses the list; the shipped `chapel` carries `['chapel', 'landmark', 'masonry', 'narrow']`, the same pair once. The same charter with `frontage: painted` builds, 2 plots of it. The gate (row 172) should take it with `suits` written once each, or drop it with that reason on `dropped`; `invalid-brief` names the owner's brief for a fault in the narrator's charter |
| 198 | Carry what the history declared and the city refused in the file, so `gb check` can print it | world, forge | `gb build` prints `Forge.build`'s `dropped` (word and reason) on every build. The file has no place for it: forge strips the dropped words from `premise.build` before `World.found`, `CitySpec` carries `premise`, `brief`, `asks` and `charters` and nothing of what was refused, and the bundle envelope is `createdWith { generator, version }`, a new field there being a `schemaVersion` 3. PLACES section 4 wants it in `gb check` too, which reads only the file. An optional `dropped: { word, reason }[]` beside `premise` on `CitySpec`, written once at `found` and read back by `World.dropped()`, is the shape `gb check` would print |
| 198 | Show `street` on `writeInstances` requests | scribe | forge 0.23.0: `InstanceRequest` extends `PlaceRequest`, so every open place carries the `street` its door is on beside `kind` and `charter`, the same field `namePlaces` already reads. Measured on three 3x3 offline towns: 23 of 23 instance requests carried one. Rows 174, 177 and 190 are closed at the forge end: 22 of 22 rooms carry `use` on the 3x3 town (`src/interior/plan.ts` writes it, a contract test holds it), `built` for a declared charter is `RECIPES[frontage][openness]` off `@gb/kitbash` (the blank rows glaze nothing at the kit's rhythm), and `PlaceRequest.street` is the band's own name |
| 199 | Every forced call on the local path comes back as the call again; measure it in a live build | scribe | host 0.6.0 (closes rows 188 and 189, one cause). Measured on llama-server b10603: it does not enforce a `response_format` grammar while `tools` ride beside it, whatever the `tool_choice` (same prompt and seed at temperature 0, step ids `step_001` and `step_0005_a` against `^step_[0-9]{4,}$` with the tools sent, `step_0001` without), so the charter ran unconstrained until the caller's 300 s (4 of 7 attempts in a live 1x1 build) and the quest ids failed the check (2 of 4 in that build, 9 of 16 replayed). A forced call on a server of your own now goes out as the tool's parameters alone, and a `pattern` the grammar cannot end (a class matching a quote, `\d`, unanchored) is left out of the grammar and checked on the reply instead. After, through the service on the build's own requests, four at once: 16 of 16 charters as the call, 26 to 38 s; 16 of 16 quests as the call, 43 to 71 s four at once, 14 to 23 s one at a time; 0 prose. A caller that hangs up (`askMs`) now frees the engine's slot within a second; until then a runaway held it to the end of the context. Nothing of a tool's `description` reaches the local engine on a forced call: what it says has to be in the messages |
| 200 | Drop `acted` off `TalkPatch` | hud | app 0.14.0 sends `does` on every turn (`does: null` takes it off), and a reopened transcript carries `does` on its turns off `@gb/talk` 0.10.0 `history()`; grep of game/app/src on 2026-08-25: 0 senders of `acted`. Rows 171 and 196 are closed on the app side |
| 201 | `addInterior` on the hand-written test rooms answers `invalid-document`, 20 cases | scene | Measured 2026-08-25 by `pnpm run verify` from the root with every box in the tree: `game/scene/tests/bar.ts:27` throws `Error: invalid-document` off `world.addInterior(interior)`, and blockers (4), pickups (6), facing (3), shell (3) and lift (4) fail on it. `@gb/world` now checks the record against the schema on `addInterior` (its contract, `World.addInterior`), so the fixture room is missing or misnaming something the schema requires; `pnpm -C game/app test` builds its rooms off a generated city and is 186 of 186 |
| 202 | The fixture person is not in the world, 48 of 49 cases | talk | Measured 2026-08-25 by `pnpm run verify` from the root: `game/talk/tests/contract.test.ts:313` throws `{"code":"broken-flow","problems":[{"where":"quest_0001","message":"giver npc_0001 is not in the world"},{"where":"step_0002","message":"npc npc_0001 is not in the world"}]}` in setup, and `Conversation.open` answers `ok: false` at `:347`. The fixture's `addNpc` (or its `addInterior`) is refused by `@gb/world`'s schema check without the test reading the result, so the person never lands; app's conversations against a generated city pass |
| 203 | Write the fixture room's ids with four digits | scene | Measured 2026-08-25 on `@gb/world` 0.12.0, where an add reads its record through the same schema `load` reads a file with: `game/scene/tests/bar.ts:17-23` writes `interior_1`, `room_1` and `door_1`, and every world id is `^kind_\d{4,}$` (`interior_0001`), so `addInterior` answers `invalid-document` at `id`, `rooms.0.id`, `doors.0.id` and `doors.0.to`, the same paths `load` refuses the document at. The same room with `interior_0001`, `room_0001` and `door_0001` is taken and the city loads (measured); nothing else in it is refused. The 20 cases of row 201 (blockers 4, pickups 6, facing 3, shell 3, lift 4) fall on that alone, so row 201 is closed on world's side |
| 204 | Mint the counter with `world.mintId('prop')` | talk | Measured 2026-08-25 on `@gb/world` 0.12.0: `game/talk/tests/contract.test.ts:33` mints the bar counter as `world.mintId('furniture')`, and a furniture id is `prop_0001` (`FurnitureSchema.id`), so `addInterior` answers `invalid-document` at `furniture.0.id`, the path `load` refuses the same document at; the fixture drops the result, the interior never lands, and both `addNpc` fall on `unknown-reference` (stationed in a missing interior), which is the "npc_0001 is not in the world" of row 202. With `mintId('prop')` the whole fixture goes in: `addInterior`, two `addNpc`, three `addItem` all ok and `check()` empty (measured). Row 202 is closed on world's side |
| 205 | Read the `Result` off `addRoad` | forge | `@gb/world` 0.12.0: `addRoad(nodes, segments)` reads the graph through the schema and answers `invalid-document` instead of writing a segment `load` would refuse (a `kind` outside `ROAD_KINDS`, `lanes` outside 1 to 4, an id of the wrong shape); `src/layout/roads.ts:34` drops the result. Measured: `pnpm -C game/forge test` is 124 of 124 on the change, so nothing it lays today is refused; reading the result is what makes a refused road visible when one is |
| 206 | Give `deed` a price and a builder, and draw the six new props | forge, furnish | Measured 2026-08-25 by `tsc --noEmit` from the root after `@gb/world` 0.13.0: `game/forge/src/prices.ts:9` (TS2741, `deed` missing from the price table), `game/furnish/src/items/index.ts:9` and `src/items/specs.ts:34` (TS2741, `deed` missing from the item builders and specs), `game/furnish/src/props/index.ts:12` (TS2740, `bars-door`, `camera`, `laptop`, `monitor`, `tablet`, `terminal` missing from the prop builders). Sizes are in `PROP_SPECS`: the four screens are `onSurface` (terminal 5x5 cells at 0.45 m, laptop 4x3 at 0.25, tablet 3x2 at 0.15, monitor 6x2 at 0.45), `camera` is `mounted` (2x2, 0.25 m, hangs on the wall behind at its `lift`, `blocks: false`), `bars-door` is 10x2 cells and 2.2 m tall, drawn at the piece's `pos` across the opening of the door it names in `doorId`. A `deed` is a paper: `HOLDING_ARCHETYPES.papers` holds it |
| 207 | Write the locks, the machines, the deeds and the owners the second wave asks for | forge | `@gb/world` 0.13.0, CONTRACT "Locks, keys, cards and deeds" and "Machines, cameras and bars". A room marked `shut` in an `admitted` charter takes `Door.locked: true` plus one of `keyItemId` (a `key` or `keycard` item), `password` (free text, 60 characters, the quest hands it out) or a `keycard` elsewhere with `opens: { doorId }`; a `private` charter locks the street door and a card with `opens: { interiorId }` is the card for that home. `check()` refuses a locked door nothing opens (`inconsistent-world`, "locked with nothing that opens it"). A screen is one of `MACHINE_PROPS` (`terminal`, `laptop`, `tablet`, `monitor`) standing on a desk (`on`, `lift`) and carries `machine: { id: world.mintId('machine'), program, locked?, password? }` with `program` in `MACHINE_PROGRAMS` (`ledger`, `camera-feed`, `mail`, `snake`, `tetris`, `blank`); a `camera` carries `watches` (a room of the interior) and a `lift`; a `bars-door` carries `doorId`. A home for sale is `Interior.forSale` (whole credits) with a `deed` item carrying `deedTo: interiorId` on a counter at that `value`; a person's home is `Interior.owner: npcId`, the player's from generation is `owner: 'player'` (`PLAYER`). The instance brief ("a house with N people, an access door and a terminal") now has shapes to land in. The shipped `station` preset carries `transit: 'subway'`, so the golden hash of a city that declares the presets moves by that one field |
| 208 | Name what a reward hands out off `@gb/world`, and ask the two new questions | quest | `@gb/world` 0.13.0 CONTRACT "What a quest may hand out, from here": an access reward is an `Access` (`{ doorId }` or `{ interiorId }`, `AccessSchema` and `accessContract`), a car reward is a `CarModel` from `CAR_MODELS` (the seven cars `cars.glb` ships), an item reward an item id, credits a number. `questView(world)` now answers `hasDoor(doorId)` and `hasMachine(machineId)` beside the five, so `WorldView` may widen to validate a lock, an access reward or a hack step (`machine_0001` ids, `password` as free text on the door or the machine) against the city. Minigame scores are the playthrough's (play), the world holds none |
| 209 | Cite `CAR_MODELS` from `@gb/world` | traffic | `@gb/world` 0.13.0 publishes `CAR_MODELS` (`NormalCar1`, `NormalCar2`, `SUV`, `Taxi`, `SportsCar`, `SportsCar2`, `Cop`) and `CarModel`, because a quest reward in a world file names a car and traffic depends on world. Hypothesis, not measured: `game/traffic/src/settings.ts` holds the same seven words; read them off world the way nav reads `CELL_KINDS` (row 165) so the two cannot drift |
| 210 | Buy the deed through `player.buy`, then `world.recordOwner(interiorId, PLAYER)`, and write the city back | app, play | `@gb/world` 0.13.0: a `deed` item carries `deedTo` (the interior) and `value` (its price); `world.recordOwner(interiorId, PLAYER)` writes `owner: 'player'` and drops `forSale`, and `world.home()` / `world.homes()` hand the player's places back. Whose a place is lives in the world file, so a deed bought changes the city: the shelf entry's city is exported again after the purchase or the home is gone on the next open. What the player put in their home (things bought and placed) is playthrough state for play, not the file |
| 211 | Cut the edge through a locked door, and block a bars-door | nav, app | PLACES stage 3 on `@gb/world` 0.13.0: `Door.locked` is the state, `world.door(doorId)` finds it anywhere with its interior, and a `bars-door` piece stands across the door it names in `doorId` at the door's `pos` (`PROP_SPECS['bars-door']` blocks, 1 m wide). What opens it is `keyItemId` (in the inventory), `password` (typed) or a `key` or `keycard` in the inventory whose `opens` names the door, or the interior when the door is the street door. Whether the player has got past a lock is play's |
| 212 | List `world.stations()` on the map for fast travel | app, hud | `@gb/world` 0.13.0: `stations()` is every plot whose charter carries `transit: 'subway'` (the shipped `station` preset does), and `plot.entrance.cell` is the subway entrance the player walks up to and clicks. The map lists them by `plot.name`; the ride and the loading are the app's, the fare the playthrough's |
| 213 | Open the machine the player sits at | app, hud | `@gb/world` 0.13.0: every `terminal`, `laptop`, `tablet` and `monitor` piece carries `machine: { id, locked, password?, program }`; `world.machine(machineId)` finds the piece with its interior. `program` is what the screen runs (`ledger`, `camera-feed`, `mail`, `snake`, `tetris`, `blank`); a `camera-feed` shows the rooms the interior's cameras `watch`. A locked machine opens on its `password` or a hack; scores and what was hacked are play's |
| 214 | `schema/bundle.json` regenerates from the world schema | bundle | `pnpm run generate` from the root rewrites it off `@gb/world` 0.13.0 (`Door.password`, `Furniture.machine`, `watches`, `doorId`, `Interior.owner`, `forSale`, `Item.opens`, `deedTo`, `Charter.transit`, six props, the `deed` archetype), and it lands in the same commit as `game/world/schema/world.json`. Every field is optional and a file from before loads unchanged: the sealed fixture still hashes to its seal (measured) |
| 215 | Drive `CityNav.setLocked` from the key check, and ask `reachableRoom` before promising a room | play, forge | `@gb/nav` 0.3.0: `CityNav.from(world)` reads every `Door.locked` into a room graph; `reachableRoom(world, from, interiorId, roomId)` is false for a room only reachable through a locked door, `setLocked(doorId, false)` restores the edge once the player has got past it (key in the inventory, password typed), `locked(doorId)` reads the state back. Play owns when a lock opens; nav only keeps it. A quest step that sends the player to an anchor in a room behind a lock should be checked with `reachableRoom` with the lock's door unlocked only once the key step is done |
| 216 | Hand an access reward, a car reward and a password out through play | quest | play 0.7.0: an access reward is `player.grant(access)` with the `Access` (`{ doorId }` or `{ interiorId }`) the world names, a car reward is `player.keepCar(model)` with the `CarModel` name, and a password a quest gives out is `player.learn(password, { questId })` (60 characters, answers whether it was new). Conditions have `player.opens(access)`, `player.knows(password)` and `player.bestScore(machineId, game)` (a whole number or nothing) to read, so a "beat the game" step can gate on a score. Row 208 |
| 217 | Drive the locks, the deed, the home, the garage and the scores off play | app, hud | play 0.7.0. A card picked up: `player.take(itemId, { opens: item.opens })` off `@gb/world`'s `Item.opens`; the access leaves with the card on `drop` or `place`. At a locked door: open when `player.has(door.keyItemId)`, `player.opens({ doorId })`, `player.opens({ interiorId })` for a street door, or `player.knows(door.password)`; a password typed right is `player.grant({ doorId })` then `nav.setLocked(doorId, false)`, and on opening a save walk `player.keys()` and unlock each `doorId` (and each `interiorId`'s street door) in nav the same way, which closes row 215 at play's end. A deed: `player.buy(deedId, value)`, `player.own(deedTo)`, `world.recordOwner(deedTo, PLAYER)` (row 210); `player.placedIn(home)` lists what to dress the home with. A car reward lands in `player.cars()`; `player.carOut` is the model to spawn at the kerb on reopen, `takeOutCar` / `putAwayCar` from the garage window (`missing-car` for one not kept). A game on a machine: `player.recordScore(machine.id, machine.program, points)` when it ends (true on a new best), `player.bestScore` on the screen. Hud's inventory window can list `player.keys()` (by what each opens) and `player.passwords()` under the things carried |
| 218 | Hold a password a person gives out with `player.learn(word, { npcId })` | talk | play 0.7.0: a password told in conversation is kept once, trimmed, with the person as its giver; `learn` answers whether it was new, so the turn can say so. What makes a person give one up is talk's (a move the quest step makes legal), the same as a fact `reveals` |
| 219 | Take a companion through the door with `crowd.visit` and bring them out with `crowd.leave` | app | crowd 0.10.0: `crowd.visit(npcId, { interiorId, at: { x, z }, heading? })` stands a companion on `at` in the interior's own metres (floor at zero) in a standing idle off the cast's `stand` shelf, on the one body they walked in with; `crowd.leave(npcId)` puts that body on the plot's doorstep. `new SceneCast(cast, root, { root: (interiorId) => <the built interior's root, or undefined> })` is what moves the body under the room; without it the body is hidden while inside. `following()` carries `interiorId` while they are in, so the four-metre arrival check reads street metres only when it is absent. The companion stands in the room with the player rather than at the door outside |
| 220 | Publish where a visitor may stand in a built interior | scene | crowd 0.10.0 takes `{ x, z }` in the interior's own metres plus an optional three.js `heading` (`Visit` in game/crowd/src/ports.ts) and places the body there with the floor at zero; it does not read `blockers` or `anchors`. A `visitor` (or a short list) on `buildInterior`'s result, a clear cell inside the entrance out of the doorway and off every `blockers` footprint, facing `inward`, is the shape the app would pass straight through |
| 221 | Cut a pack with `gb extend` and apply one with `gb apply` | cli | `@gb/bundle` 0.5.0: `Pack.cut(base, { world, quests })` after `Forge.extend(world, count)` writes [schema/pack.json](../game/bundle/schema/pack.json), holding the base's world id and content hash and only what the growth added (plots, interiors, people, items, placements, cells built on, counters, charters, quests); `Pack.apply(base, packFile)` answers the extended city as an `OpenedBundle`, which `Bundle.pack(opened.world, opened.quests, { requires: opened.requires })` writes to disk. `extend` writes into the `World` it is given, so `gb extend` opens the base file once for `base` and once more for the world it grows. Errors past `open`'s: `not-an-extension` (paths), `invalid-pack` (paths), `pack-mismatch` (the base the pack names against the one given), `content-changed`. Measured on a 3x3 town at density 0.5: 23 plots, 2 interiors, 5 people, 3 items in the pack, applied back byte for byte, twice to the same hash |
| 222 | Add a pack to a city on the shelf | app | `@gb/bundle` 0.5.0: `Pack.apply(shelfCity, packFile, have)` gives the extended city as an `OpenedBundle` with a new `contentHash`; put it on the shelf as its own entry or in the base's place, and export it the way any city is. A save written against the base resumes in the extended city with `report.rebuilt: true` and `dropped` empty, because every base id is still there; the panel can say the city grew rather than that anything was lost. A pack file that names another city is `pack-mismatch`, one edited after it was cut is `content-changed` |
| 223 | Write the quests for what `extend` added | forge | Measured 2026-08-25: `Forge.extend(world, 30)` on a 3x3 town at density 0.5 adds 23 plots, 2 interiors, 5 people and 3 items and writes no quests, and nothing in the contract writes quests for a grown city. A pack is a town without work until it has some: an entry that writes quests for the new places against the whole city (`summarise` already reads it), ids continuing from the base's quest list (`quest_0005` after four), naming base people and places freely, is what `gb extend` needs. `extend` also writes into the `World` it is given and answers a `Result`; both are as `@gb/bundle` reads them |
| 224 | Make `reward.access` optional, not defaulted | quest | Measured 2026-08-25 on the uncommitted `schema/quest.json`: `reward.access` carries `default: []`, so parsing a quest written without it adds the field, and `@gb/bundle` hashes the parsed body: the sealed fixture hashes to `42370b11...` instead of its seal `34102454...` and answers `content-changed`, and every bundle written before today does the same (6 of 22 bundle tests fail on it, the 3 pack tests pass). The contract's own rule is that new fields go on as optional because exported worlds carry quests written without them; `.optional()` in place of `.default([])` puts every hash back |
| 225 | Publish the world document's record schemas | world | `@gb/bundle`'s pack schema (`src/pack/schema.ts`) checks a pack's plots, interiors, npcs, items, placements and counters with the world's own record schemas, read off `worldContract.schema.shape` behind an `instanceof z.ZodObject` guard because only `worldContract` is exported. Exporting them by name (`PlotSchema`, `InteriorSchema`, `NpcSchema`, `ItemSchema`, `PlacementSchema`, `IdCountersSchema`) is what lets that read be an import |
| 226 | Drive `unlock`, `hack`, `beat-game` and `buy` in the harness, and write them | forge | quest 0.9.0: `unlocked { doorId }`, `machine-unlocked { machineId }`, `scored { machineId, score }` (credited at or past the step's `score`), `bought { itemId }` (counted like `acquired` on a `collect`; `acquired` never credits a `buy`). A `give-password` effect (`password`, 60 characters) is how a quest hands the word out; the log writes it with `player.learn(password, { questId })`. Rewards: `access: Access[]` (`{ doorId }` or `{ interiorId }`), `car: CarModel`, `deed: interiorId`, every id checked against `questView(world)`; `REWARD_TABLE` allows 0/1/2/2/3 accesses per tier, a car from `hard`, a home at `epic`. The objective carries `doorId`, `machineId` and `score`; `world.door` and `world.machine` say where they stand |
| 227 | Send the four lock and counter events, and write the deed into the city | app | quest 0.9.0: send `unlocked { doorId }` when a door's lock comes off (key in hand or password typed), `machine-unlocked { machineId }` when a screen's lock comes off, `scored { machineId, score }` when a game on it ends, and `bought { itemId }` beside `acquired` on a purchase. On `quest-complete`, `reward.deed` is the interior for `world.recordOwner(deed, PLAYER)` (the log already wrote `player.own`); access, the car and the password reach the player through the log (`grant`, `keepCar`, `learn`). Measured 2026-08-25: `tsc --noEmit -p game/app` fails at `tests/contract.test.ts:46` and `tests/playing.test.ts:267`, hand-made `WorldView`s missing `hasDoor` and `hasMachine`; `questView(world)` answers both |
| 228 | Answer `hasDoor` and `hasMachine` off the summary, and show the writer the locks | scribe | Measured 2026-08-25 by `tsc --noEmit` from the root: `src/quests.ts:153` (`SummaryView` missing `hasDoor`, `hasMachine`) and `tests/quests.test.ts` at 120, 157, 171 and 197. The summary the quest writer reads has to list each locked door (id, what opens it: key item, password or card) and each machine (id, program, locked) for the writer to use `unlock`, `hack`, `beat-game`, `give-password` and the access, car and deed rewards; a lock step naming a door the view denies is refused as not in the world |
| 229 | Widen the fixture world | talk | Measured 2026-08-25 by `tsc --noEmit` from the root: `tests/contract.test.ts:306`, a hand-made `WorldView` missing `hasDoor` and `hasMachine`; `questView(world)` answers both |
| 230 | Draw the lock, the machine, the score and the new reward kinds | hud | quest 0.9.0: a step line may carry `doorId`, `machineId` and `score` (the score to reach) beside the older targets; `quest-complete`'s `Reward` carries `access` (`{ doorId }` or `{ interiorId }`), `car` (a model name) and `deed` (an interior) beside money, standing and items, for the announcement and the journal |
| 231 | Regenerate `schema/bundle.json` off quest 0.9.0 | bundle | `pnpm run generate` from the root embeds `@gb/quest`'s schemas: four step kinds (`unlock`, `hack`, `beat-game`, `buy`), the `give-password` effect, `reward.access`, `reward.car`, `reward.deed` and four events. Every field is optional or defaulted, so a file from before loads unchanged |
| 232 | Leave the ground open under a station's doorstep cell | scene | `@gb/kitbash` 0.10.0 draws a subway entrance into every building whose charter carries `transit: subway`, on `plot.entrance.cell`: a stairwell 1.5 by 1.6 m going 1.44 m down, walled either side, with a lip out to the cell's edge and a lit box over its back wall. Measured on the placeholder kit: the building's kit-material meshes reach exactly one cell past the front and to y -1.49. The pavement is laid over the cell and the street skin 2 cm above it, so everything under y 0 is behind them and only the balustrade and the box show; a cell skipped from the ground quads, the skin, the doorstep marking and the clutter for each of `world.stations()` shows the stairs. The well covers the whole cell, so nothing under it needs filling |
| 233 | Draw the entrance and the camera on pack buildings | prefab | `@gb/kitbash` 0.10.0: `fixturesFor(plot, size, charter)` says where a station's subway entrance (`subway.position`, the doorstep cell centre, `rotationY`, `cellSize`) and a private place's camera (`cameras[]`, on the front over the door) stand in the building's frame, and `fixtureParts(fixtures)` hands their geometry in that frame, one buffer per kit material name (`MI_Trim_MetalConcrete`, `MI_Trim`, `MI_Asphalt`), in the shape a kit part is. The entrance's lit box is in `signsFor` as `kind: subway` and in `lightsFor` beside it. Hypothesis, not measured: a station with a design in the file is built by prefab and gets none of it today |
| 234 | Walkers cross the station's balustrade | crowd | Hypothesis, not measured: the doorstep cell of a station is pavement to the grid, and `@gb/kitbash` 0.10.0 stands two walls 0.95 m tall along its sides and a 1.4 m back wall against the building on it (`SUBWAY`, the well 1.5 m wide and 1.6 m long centred on the cell, open at the street end), so a pedestrian routed over the cell walks through them. The stations are `world.stations()` and the cell is `plot.entrance.cell` |
| 215 | Push the counter and route `buy` | app | `@gb/hud` 0.11.0: `hud.show({ counter: { seller, offers: [{ id, name, price }] } })` when the player uses a counter (row 150: every item on a `serve` anchor with `ownerNpcId` and `value`), with `money` pushed as it is now; the hud disables Buy on anything over the money pushed and reports `{ kind: 'buy', itemId }` on a click. Route it to `player.buy(itemId, item.value)`, then push `money`, `carrying` and the counter again without the thing sold: the hud takes nothing off the counter itself. `counter-closed` comes from Escape and the close button; `counter: null` closes it from the game. `Carried.value` is drawn on inventory rows, so push `item.value` with `carrying` |
| 216 | Open the machine on `screen`, route `unlock` and `score` | app, play | `@gb/hud` 0.11.0: `hud.show({ screen: { machineId, title, locked, program } })` for the piece `world.machine(id)` finds (row 213); `program` is `{ kind: 'text', title, lines }` for `ledger`, `mail`, `camera-feed` and `blank` (the generator's text, the watched rooms as lines), or `{ kind: 'snake' \| 'tetris', best }`. Locked, the hud reports `{ kind: 'unlock', machineId, password }` on Enter; answer with `locked: false` or `refused: true` (a hack that succeeds is the same `locked: false`). A game ending reports `{ kind: 'score', machineId, game, score }` once: keep the best per machine in play and push it back as `best` (a push of `best` never restarts the game). Every key is the screen's while it is up and `hud.typing` reads true, so the walk keys are already off; `screen-closed` carries the machineId and `screen: null` closes it from the game |
| 217 | Push `stations` and `boarding` on the map, and route `travel` | app | `@gb/hud` 0.11.0: `map.stations` is `world.stations()` as `{ id: plot.id, name: plot.name, x, y }` in cells (row 212), drawn as squares on the plan and listed beside the bearings; `map.boarding` is the plot id of the station whose entrance the player is at, which is when the other stations carry a Travel button reporting `{ kind: 'travel', stationId }`. On it: push `window: null` and `loading: { title: 'Riding to <name>', stages: [] }` (no stages draws the veil), move the player, then `loading: null` |
| 218 | Push `homes` from `world.homes()` and `player.placed()` | app | `@gb/hud` 0.11.0: `hud.show({ homes: [{ id, name, text, placed: [{ id, name, value }] }] })` for every interior whose owner is the player (row 210), `placed` being `player.placed()` filtered to that interior and named off the world; drawn under the inventory as "Your places". An empty list draws "No place of your own yet.", so push it from the start |
| 219 | Credits are the word everywhere the hud counts money | app | `@gb/hud` 0.11.0 writes "credits" in the inventory, on a price, and on a money notice ("+40 credits"); an app test that reads "coin" off the hud reads "credits" now. `TalkPatch.acted` is gone (row 200 closed): `does` is the only name |
| 220 | Publish `shell(plot, size, charter)` beside `building`: the walls and roof with nothing on them that is only read from the pavement | kitbash | `@gb/scene` 0.13.0 batches every plot's `shell` at open and asks `building` (and `lights` after it) only for plots within `DETAIL_RADIUS` (64 m) of the player's cell, taking them out again when the player walks off; a dressing with no `shell` is built whole at open for every plot, as before. A kit shell is the welded wall pieces on the kit materials with no sign meshes and the panes on a flat glass (no room attributes), so the far town is one draw per kit material with no raymarch. Measured on a 20x20 town (2,607 plots) with a greybox that hangs a sign per storey, a screen and three panes per storey per wall: open 2,100 ms to 469 ms, triangles at the spawn 185,216 to 92,444, emitters 10,081 to 109, one more draw per material near the player |
| 221 | Publish `shell(plot, size, charter)` beside `building`: the model on a material with no interior mapping and no screens | prefab | Same seam as row 220. The pack draws a whole building as one mesh on `Library.material`, so a shell is that mesh on a second material (`wall:` and `base:` read as plain pictures, the windows unlit, the screens dark) or the same material with the raymarch off; `@gb/scene` batches shells into `city:<material>` and the near detail into `detail:<material>`, so a far building costs the picture fetch alone. `lights` is asked after `building` only, never after `shell` |
| 222 | Call `city.follow(x, z)` every frame in place of `city.lights.follow`, and take rooms from `city.interior(interiorId)` | app | `@gb/scene` 0.13.0: `follow` hands the lights to the nearest emitters as before and, when the player's cell changes, dresses the buildings within `DETAIL_RADIUS` in detail, drops the far ones to their shells and lets go of rooms whose building is beyond it (`InteriorBuild.dispose`). Indoors, give it the door the player came in by, as the lights already get. `city.interior(id)` builds a room on first entry and keeps it while near, so a re-entered room is the same `InteriorBuild` (its pickups as the player left them) until they have walked `DETAIL_RADIUS` away, after which the next entry is the file's room and `player.placed()` is applied again as on a first entry (row 80). Measured on a 20x20 town: a same-cell `follow` is 2 to 4 us, a cell crossing 0.16 to 0.65 ms median and under 5 ms at worst, the cost being the dressing's `building` for the one to three plots that crossed the radius |
| 223 | Stand a companion who came in with the player on `room.visitorCells[n]` | app | `@gb/scene` 0.13.0 `InteriorBuild.visitorCells`: `{ x, z, roomId }` in interior metres, nearest the street door first, every 1 m square of floor a 0.7 m body fits in clear of the blockers, 1.5 m from every door, 0.7 m from every anchor and off the strip the staff work along behind their counter, desk, bench or stove. The first companion takes the first cell, the second the second. Measured over the 1x1 fixture town, 7 rooms: every one offers at least one |
| 224 | A two-handed thing is left on a bar stool it cannot fit on | forge | Measured on 2026-08-25 in `@gb/scene`'s fixture towns (`Forge` offline, seeds `scene` 1x1 and `ceiling` 5x5): `interior_0005 item_0009`, a `statue` carried `two-handed` (a 44 cm crate), is placed on the `sit-drink` anchor of a `bar-stool` (`PROP_SPECS`: 40 by 40 cm), and `interior_0014 item_0030` the same. Scene stands a thing wider than its piece in the middle of it, so the crate overhangs the stool on every side. A thing left on a seat wants to fit the seat: `footprintOf(prop)` against the item's bulk (20 cm pocket, 35 cm bag, 44 cm two-handed) at placement |
| 225 | Draw the four screens on their hosts, the camera on the wall and the gate of bars across its door | furnish | `@gb/forge` 0.24.0 writes them into every town it builds. A screen (`terminal`, `laptop`, `monitor`, `tablet`) carries `on` (a `desk`, a `counter` or a `bar-counter`) and `lift` at that host's top (`tableHeight` 0.75 for a desk, 1.0 for a counter), its `rot` facing whoever uses it: the office chair drawn up to a desk, the customer side of a counter; it sits over the far half of the top from them, the bar's game monitor beside the till, 0.1 m in from the counter's end. A `camera` carries `lift` 3.5 (its top a hand under the 4 m ceiling), `rot` inward from the wall it hangs on, `watches` the room it hangs in, in the corner at the start of the entry room's back wall; it claims no floor. A `bars-door` stands at the locked door's own `pos` and `rot`, in the room the door opens into, `doorId` naming it, only in `civic`, `industrial` and `worn` finishes. Measured on an 8x8 offline neon town: 99 screens, 5 cameras; on the 3x3 `club-1` town the disco has one gate, one camera, one snake screen |
| 226 | Dress the disco's dance floor | furnish | Measured on the offline 3x3 neon town `club-1` (`Forge` + `OfflineNarrator`, theme `dense neon port city`): `Cass's Club` is a `disco` charter (`finish: worn`, `access: admitted`) whose main room is a `taproom` named `Dance floor` with 2 `dance` anchors on the open floor between the bar and the tables (each keeping 0.9 m of floor), a bar counter with stools, one or two bodies propped on the free walls, a jukebox some of the time, a locked cellar behind a gate of bars. The room reads as a bar with dancers today; the floor under the dance anchors wants a dressing of its own (a lit floor, a darker finish round it) read off the `worn` finish and the `dance` anchors in the room, since `taproom` is the use it carries. A `dance-floor` room use is asked of world in row 228 |
| 227 | Write the instance brief into the place prompt, and the locks, screens, sales and prices into the quest prompt | scribe | `@gb/forge` 0.24.0: every `InstanceRequest` carries `has: InstanceBrief`: `locked` (rooms behind a lock by name, each `by: 'key' | 'card' | 'code'`), `machines` (`{ room, program }`, program one of `@gb/world`'s `MACHINE_PROGRAMS`), `camera` (boolean) and `forSale?` (whole credits). The offline narrator ignores it; a place written knowing "a house with two people, a locked study and a laptop with the mail on it" is what the owner asked for (PENDING, the second wave). Keys, cards and deeds are not in `things`: forge names them off what they open or own. `WorldSummary.places[]` carries `locks` (`doorId`, `room`, `roomId`, `street`, `keyItemId?`, `keeperNpcId?`, `password?`, `behind` item ids), `machines` (`machineId`, `program`, `locked`, `password?`, `roomId`), `forSale?`, `work` (the charter's), `npcs[].roomId` and `items[].value` and `roomId`; the offline writer builds `unlock` (a `give-item` of the key or a `give-password` of the code on a `talk` first), `hack` (`give-password` first), `beat-game` and `buy` (with `money-at-least` the bill) steps and `access`, `car` and `deed` rewards on them, all of which `@gb/quest` 0.9.0 validates, so the quest tool's schema and prompt can offer the same. A person behind a lock is nobody a step may name until the door is open; forge leaves them out of its cast |
| 228 | A `dance-floor` room use | world | The disco's main room is a `taproom` named `Dance floor` with `dance` anchors on it, because `ROOM_USES` has one value per dressing routine and no routine dances. The owner's brief for the disco (PENDING, "A disco, as the first test of the charter design") asks for a dance floor room use. Forge adds the routine (a bar counter, the open floor kept for up to six dancers, lean spots, a jukebox) the day `dance-floor` is in `ROOM_USES` with `ROOM_USE_KIND['dance-floor'] = 'main'`; its `furnishRoom` switch is exhaustive over the enum and will not compile without the case |
| 229 | `deed` in `HOLDING_ARCHETYPES.papers` is never stock | world | A deed without `deedTo` fails `ItemSchema`, so a deed rolled as loose stock off `papers` would refuse the world; forge leaves `deed` out of every stock pool (`stockOf`) and writes one only when a home goes on sale. If `papers` is meant to roll deeds, the archetype needs a home to name; otherwise it belongs out of the class |
| 230 | What to do at a door, a screen, a counter, a station and a deed | app | `@gb/forge` 0.24.0 writes them; nothing in the running game acts on them yet. A door: `Door.locked`, opened by `player.has(door.keyItemId)`, `player.knows(door.password)` (typed at the door), `player.opens({ doorId })` or, on a street door, `player.opens({ interiorId })`; a code that gives is `player.grant({ doorId })` so it stays open across a save; the quest log takes `unlocked { doorId }`. A screen: `Furniture.machine` on every `terminal`, `laptop`, `monitor` and `tablet`, `locked` with a `password` unless it runs a game; sitting at it is the hud's screen, its lock checked with `player.knows`, and the log takes `machine-unlocked { machineId }`; a game reports `scored { machineId, score }` and the best goes to `player.recordScore(machineId, program, points)`. A counter: a thing with `ownerNpcId` and `value` is bought with `player.buy(itemId, value)`, and the game sends `bought { itemId }` and `acquired { itemId }` both. A deed: buying it is `player.own(item.deedTo)` and `world.recordOwner(item.deedTo, PLAYER)`, and a `quest-complete` carrying `reward.deed` is the same two calls; the player's own home is a door that opens for them whatever its lock. A car reward is `player.keepCar(reward.car)`. Keys and cards are placed `at: 'npc'` in a keeper's pocket and reach the player only through a quest's `give-item`; a key in hand is `player.take(itemId, { opens: item.opens })`. Stations are `world.stations()`; the mix rolls many of that kind (26 on an 8x8 town, 157 on a 20x20), so a map that lists every one lists a lot |
| 235 | Carry a pack's catalogues into `requires` on `apply` | bundle | Measured 2026-08-25 with `gb extend` on a base whose file names no art (`requires: []`, no catalogues): the growth is pinned to gb-buildings 1.8.0, the pack declares that catalogue in `world.catalogues`, and `Pack.apply` answers the city with `requires: []`, so `Bundle.pack(opened.world, opened.quests, { requires: opened.requires })` writes a city whose document pins buildings to a pack its envelope never names (`packs.asBuilt` false, no verdict); it hashes to `ad5f0487` where the grown file `gb extend` wrote, naming the pack, hashes to `e0a4a6f0`. A base that names the pack already is unaffected: the grown file and the applied file are byte-identical. `requires` on apply as the base's list plus every catalogue the pack declares that the base lacks is the shape `gb extend` writes |
| 236 | Say what `apply` seals the city with, or hand back the sealed document | bundle | Measured 2026-08-25: `Pack.apply`'s `contentHash` is `Bundle.pack(world, quests, { generator: pack.createdWith.generator, requires })`'s and no other (`{ generator: 'gb apply' }` and `{}` hash differently), and the contract says only "the hash `Bundle.pack` gives that city". `gb apply` reads `createdWith.generator` off the pack file to write a bundle under apply's own hash, and refuses to write when the two differ. A field on `OpenedBundle` carrying the sealed `BundleDoc`, or that sentence in the contract, makes it a promise rather than a measurement |
| 237 | Read a pack without its base | bundle | `gb check pack.json` on its own reads `format`, `base` and the list lengths off the file and cannot prove the seal: `Pack.apply` is the only door and it needs the base. A `Pack.read(value)` answering `{ base, counts }` after shape and `contentHash`, with `invalid-pack` and `content-changed`, is what `gb check` would print for a pack alone |
| 235 | Publish the harness for a city a live narrator built | forge | `pnpm --filter @gb/forge run playable [seed] [sizes] [theme]` builds its towns with the offline narrator, so a city `@gb/scribe` wrote through the sidecar has no way into `tests/playable.ts` and its completable figure cannot be measured from outside forge. An entry taking `{ world, quests }` and answering the three figures (at their posts, targets kept in, nobody kept) is what scribe's `measure` would print beside the model's quests. Until then scribe walks every shipped quest with `src/reach.ts`, its own copy of the harness's lock, screen and counter rules (a door opens only with its key in hand or its code known, nothing behind a locked door is reached before it is open, a hack needs its code, a game needs a screen that runs one, a buy needs the money), and reports how many pass that walk |
| 238 | Open the counter on `did show_wares`, and read `granted` | app | `@gb/talk` 0.11.0: `did { action: 'show_wares' }` is the person naming their stock (every thing on a surface in their building with them as `ownerNpcId` and a `value`, the counter's own list); the counter is where it is bought, so push `hud.show({ counter })` for `conversation.npcId` (row 215's shape) on that `did` and route the buy through `player.buy` as row 215 says. `granted` events, `{ keyItemId }`, `{ password }` or `{ access }`, arrive on the `say` and `choose` streams and as `granted` on the `open` result (a step credited on the way in has already paid): refresh the keys and passwords the inventory draws, and on `{ access: { interiorId } }` (a person's home opened to the player, or a reward) call `nav.setLocked(streetDoorId, false)` per row 215, since `player.opens({ interiorId })` reads true from then on. `show_wares` and `invite_home` are two more `ActionName`s a `did` can carry. Talk moves no money and no lock |
| 239 | A key handed out by `give-item` carries no access in play | quest | Read off the contracts, not measured in a running game: `give-item` lands `take(itemId)` and `@gb/play` 0.7.0 holds a key's access only off `take(itemId, { opens })`, so a quest-given key or card is in hand with `player.keys()` naming nothing it opens and `player.opens({ doorId })` false for it. The app's door check reads `has(door.keyItemId)` as well (row 230), so a door with `keyItemId` still opens; a card whose lock is only on the card's side (`opens: { interiorId }`, the shape a card for a home is written in) only reads through `opens`, and stays shut. `@gb/talk` 0.11.0 publishes `granted { keyItemId }` for such a step regardless, reading the lock off the world on its side, and takes a carried key it hands over with `{ opens }` itself. Landing the effect with the item's `opens` needs the item, which `WorldView` cannot answer today: `opensOf(itemId)` on the view, or app re-taking the key with `opens` off `world.item(itemId)` on `step-done` |
| 240 | The giver of a password a step pays out is the quest, and the person is on the event | play, quest | Closes row 218 on talk's side. `@gb/talk` 0.11.0 reads the word off the talk step's `give-password` effect; `@gb/quest` lands it as `learn(password, { questId })` when the step is credited, and `learn` keeps the first giver, so `passwords()` says the quest, and the `granted { password }` event, published in that person's conversation, says the person. A journal that wants the person's name beside the word needs the effect landed as `learn(password, { questId, npcId })` for a talk step, which is quest's to send and play's to keep; talk has no earlier moment to call `learn` with the npc without giving the word out before the step is done |
| 235 | Stand the gate open when its door unlocks, and drop its blocker | app | `@gb/furnish` 0.12.0: `dressing.opened('bars-door')` (and `kit.opened('bars-door', style)`) is the same gate with its leaf slid sideways into the wall beside the doorway, on the same material, one mesh; `prop('bars-door')` is the shut one, lamp red on the lock. On `unlocked { doorId }` for a door a `bars-door` names, put the open one in the shut one's place (same position, lift and turn) or swap the mesh's geometry, and take the gate's rectangle out of `blockers`: `@gb/scene` measured it off the shut object, and the posts still stand, so the open gate's box is wider, not gone |
| 236 | Place a `tablet` somewhere | forge | Measured 2026-08-25 over four generated 3x3 towns (`furnish`, `furnish-2`, `furnish-3`, `club-1`): 24 terminals, 5 laptops, 4 monitors, 0 tablets. `PROP_SPECS.tablet` is 3x2 cells at 0.15 m, `onSurface`, and furnish draws it on a stand with its program printed on it like the other three; a reception counter, a cafe's order pad or a market stall would carry one |
| 237 | `dance-floor` in `ROOM_USES` lights the whole floor | world | Row 228 stands. `@gb/furnish` 0.12.0 reads it ahead: a room whose `use` is `dance-floor` is dressed as a dance floor whether or not anybody is dancing in it yet (every clear tile lit, the booth on the wall nearest the middle), and a room with `dance` anchors is dressed the same today off the anchors alone, so the day the value lands nothing in furnish moves |
| 241 | `CastMember` grew `build`, `speak`, `pulse` and `speaking`: put them on the cast stub | crowd | Measured 2026-08-25 by `pnpm run typecheck` from the root on `@gb/cast` 0.10.0: `game/crowd/tests/support/stub-cast.ts(19,14)` TS2420 and `(87,5)` TS2739, `Type 'StubMember' is missing the following properties from type 'CastMember': build, speak, pulse, speaking`. `build` is `'regular' | 'heavy'` (a field, `'regular'` will do for a stub), `speak(on: boolean)` and `pulse()` return nothing and `speaking` is a boolean. `CrowdActor` is unchanged, so nothing crowd drives moves |
| 242 | Speak a reply through the body: `speak(true)`, `pulse()` on every chunk, `speak(false)` at the end | app | `@gb/cast` 0.10.0 replaces the manual talk gesture (`game/app/src/gestures.ts`, contract line "Whoever is speaking talks with their hands") with speech timed to the words: `member.speak(true)` when the reply starts arriving, `member.pulse()` on each `replyChunk`, `member.speak(false)` when the turn closes. The hands come up to a third of their weight while the line is waited for and rise toward full weight as the chunks arrive; the head beats 3.5 degrees of nod at 3.1 Hz scaled by how much is arriving, and goes still within a second of the chunks stopping. It picks the standing or seated talk off the clip the body is holding and follows a clip change, so nothing needs to be chosen by the caller, and a nod or a shake of the head still goes over it with `gesture`. It is mimicry, not lip sync (the owner, 2026-08-25: "lypsinc IS TOO MUCH!! just mimic speaking is enough"). Same release: `game/app/tests/starting.test.ts(63,3)` TS2739 wants `build`, `speak`, `pulse` and `speaking` on its cast stub |
| 243 | Say which piece a screen sits on, and which locked door has a gate across it | forge | `InstanceBrief.machines` is `{ room, program }` and `locked` is `{ room, by }`, so `@gb/scribe`'s place prompt can say "a screen in the office running the mail" and never "the terminal on the back desk" or "the gate of bars across the cellar door". The plan already chooses both (`stampMachines` picks the piece `@gb/furnish` draws, and a locked inner door in a `civic`, `industrial` or `worn` finish gets a `bars-door`), so a `piece` on each machine and a `bars` flag on each lock would reach the writer that describes the room. Measured on the live 3x3 town (`pnpm --filter @gb/scribe run measure 1 3 16 10`): 8 screens and 3 locked doors, every brief line written without the thing the player walks up to |
| 244 | A locked door has no way to type a word at it | hud | `@gb/app` 0.15.0 opens a locked door for `player.knows(door.password)`, which is the word a job handed out, because `ScreenView` is the only password field in the interface and it is machine-shaped (`machineId`, a `program` to run once it opens). A door panel with the same lock line as the screen (a name, a masked field, `unlock { doorId, password }`, answered with an open or a `refused`) is what typing a word at a door needs; until then a player who was told a word never types it and a word learned outside a quest opens nothing |
| 245 | Write the city back to the shelf after a deed is bought | app | Row 210's second half. `@gb/app` 0.15.0 writes the owner into the running world (`world.recordOwner(deedTo, PLAYER)`) and mends it on the way in from `player.owned()` (`src/rewards.ts` `restore`), so the running game and the save agree. The document on the shelf and the file Export writes are still the ones the city was opened as, so a city shared after a purchase is shared for sale. Re-sealing the world and re-filing the shelf entry is `src/boot/`'s |
| 246 | Stand the gate open in the room, not only out of the way of the player | app | Row 235 is half closed: `@gb/app` 0.15.0 takes the `bars-door` rectangle out of `@gb/scene`'s blockers and stands its own across the door while the lock is on, so an unlocked gate is walked through with no room rebuilt (`src/solids.ts` `gated`). The shut mesh is still the one drawn: `RoomArt` (`src/pack.ts`) hands back `{ dressing, decor }` and drops `FurnishDressing.opened`, so nothing in the running game can put the open gate in its place. Widening `RoomArt` with `opened(prop)` and swapping the object in `InteriorBuild.props` on `unlocked` is the rest of it |
| 247 | `CastDressing` drops `shell`, so a town dressed with people in it is dressed whole at every distance | cast | Measured 2026-08-25 in Node against `@gb/cast` as it stands: `new CastDressing(cast, inner)` over an inner dressing carrying `shell`, `lights`, `marking` and `clutter` answers `'lights' in dressing` true and `'shell' in dressing` **false**. `@gb/scene` 0.13.0 reads `shell` by asking whether it is there, so the whole chain the app composes loses the far look at this link and every plot in town is asked for its whole `building` at open. Forward it beside `lights`, `marking` and `clutter`; the contract line "and `lights`, `marking` and `clutter` are there when that dressing has them" is the sentence that needs `shell` in it. `@gb/app` composes round it today by taking `shell` and `lights` off the building dressing directly (`src/pack.ts` `overBuildings`), which goes the day both links carry them |
| 248 | `FurnishDressing` forwards neither `shell` nor `lights` from the dressing behind it | furnish | Same measurement, same day: `new FurnishDressing(furnishKit(), inner)` answers false for both, where the inner carries both. The furniture layer answers for rooms and hands `building`, `ground` and the rest through, so a city dressed as `FurnishDressing(kit, prefab)` loses the far look of every building and every emitter `@gb/prefab` publishes: measured on a 20 by 20 town through the app's chain, **0 emitters against 34** and every plot dressed whole at open (860 ms against 653). Forward both to `rest` the way `building` is forwarded |
| 249 | `city.interior(id)` builds with the city's dressing, and a room is dressed for its own building | scene | `@gb/scene` 0.13.0 owns the rooms, which is right, but the only dressing it can build one with is the one `buildCity` was given, while `@gb/furnish` publishes a dressing and a decor mesh **per interior** (its finish, its surface choices, its screening, its wall bays). `@gb/app` 0.16.0 hands `buildCity` a seam that stands aside for one interior's own art for the length of the `city.interior(id)` call (`src/rooms.ts`), which works because a room is only ever built inside that call and it returns before anything else runs. A second argument, `city.interior(id, dressing?)`, would say it outright instead of relying on that; so would a `dressing(interiorId)` hook on `CityOptions` |
| 250 | `Pack.apply` sealing back to its own hash holds from the app as well | bundle | Rows 236 and 235 read from the browser side. Measured 2026-08-25 in `@gb/app`: `Bundle.pack(applied.world, applied.quests, { generator: pack.createdWith.generator, requires: applied.requires })` reopens to exactly `applied.contentHash` for a pack cut in the browser, so the panel can shelve and export the city an applied pack gives. It is a measurement and not a promise: the app refuses the pack when the two hashes differ rather than shipping a city nobody else would open, and a field on `OpenedBundle` carrying the sealed document (row 236) would take that check out |
| 251 | The settings tab is the clock and the sky, so the player's own screen source lives on the boot panel | hud | `@gb/app` 0.16.0 takes an address for a video the televisions play. `SettingsView` is `{ hour, minute, locked, weather, weathers }` and `HudIntent` has `lock-time`, `skip-time` and `weather`, so there is nowhere in the running interface to type one: the field is on the panel, under "Your settings", and the game is handed it at start. A `screens` line on `SettingsView` (the address as text) with a `screens { source }` intent would put it where the player already goes to change the weather, and the app would swap the source without leaving the city |
| 252 | Walk the town's doors off `world.charters()`, not `BUILDING_KINDS` | crowd | `@gb/world` 0.14.0 deleted `BUILDING_KINDS` and `BuildingKind`: a city's kinds of place are whatever its charters declare, so a fixed word list would miss every invented one. Measured 2026-08-25 by `pnpm run typecheck` from the root: `game/crowd/src/places.ts:2` fails TS2305 (no exported member `BUILDING_KINDS`). `world.charters()` answers `ResolvedCharter[]` for this city (its own list when the file carries one, else `SHIPPED_CHARTERS`), `charter.word` is the word, and `world.plotsOfKind(word)` takes any word. Contract line 62 of `game/crowd/CONTRACT.md` names `BUILDING_KINDS` too |
