# research store (may contain proprietary insights, default private)
.research/

node_modules/

# build output, anchored: an unanchored rule also swallows source folders that
# happen to be called build/ or dist/, and it does it silently
/target/
/assets/build/
/assets/dist/
/game/*/dist/
*.gguf
*.onnx
*.safetensors
.env

# what the sidecar's settings screen writes: the keys, and everything that is
# not a key. Both belong to this machine, neither is shared
.env.local
.game-box.json

tsconfig.tsbuildinfo
assets/src/

# a pnpm store a container left behind when it installed over the mount
.pnpm-store/

# cities built locally to look at, not shipped
/cities/
